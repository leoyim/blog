<rss xmlns:atom="http://www.w3.org/2005/Atom" version="2.0">
    <channel>
        <title>所有文章 - LeoYim&#39;s Blog</title>
        <link>http://leoyim.cn/posts/</link>
        <description>所有文章 | LeoYim&#39;s Blog</description>
        <generator>Hugo -- gohugo.io</generator><language>zh-CN</language><managingEditor>leo.yanwb@outlook.com (leoyim)</managingEditor>
            <webMaster>leo.yanwb@outlook.com (leoyim)</webMaster><copyright>This work is licensed under a Creative Commons Attribution-NonCommercial 4.0 International License.</copyright><lastBuildDate>Wed, 23 Sep 2026 09:44:00 &#43;0800</lastBuildDate><atom:link href="http://leoyim.cn/posts/" rel="self" type="application/rss+xml" /><item>
    <title>Jev AI 决策模型深度分析报告</title>
    <link>http://leoyim.cn/jev-ai-analysis/</link>
    <pubDate>Wed, 23 Sep 2026 09:44:00 &#43;0800</pubDate>
    <author>作者</author>
    <guid>http://leoyim.cn/jev-ai-analysis/</guid>
    <description><![CDATA[<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Jev AI 决策模型深度分析报告 | TypeSafe AI</title>
<style>
  :root{
    --bg:#f5f6f9;
    --surface:#ffffff;
    --surface-2:#f0f1f7;
    --border:#e2e4ec;
    --text:#1a1d24;
    --text-2:#4a5163;
    --text-3:#767e91;
    --brand:#5b5bd6;
    --brand-soft:#eef0ff;
    --cyan:#0e7fa8;
    --cyan-soft:#e7f5fb;
    --red:#c0392b;
    --red-soft:#fdeeec;
    --amber:#a86a10;
    --amber-soft:#fdf4e6;
    --green:#1c7f5a;
    --green-soft:#e9f6f1;
    --shadow:0 1px 2px rgba(20,25,40,.04),0 6px 18px rgba(20,25,40,.05);
    --radius:12px;
  }
  *{box-sizing:border-box}
  html{scroll-behavior:smooth}
  body{
    margin:0;
    background:var(--bg);
    color:var(--text);
    font-family:system-ui,-apple-system,"Segoe UI","PingFang SC","Microsoft YaHei",sans-serif;
    font-size:15px;
    line-height:1.75;
    -webkit-font-smoothing:antialiased;
  }
  code,pre,.mono{font-family:ui-monospace,"Cascadia Code",Consolas,"Liberation Mono",monospace}

  /* ---------- Hero ---------- */
  .hero{
    background:linear-gradient(135deg,#4b4bc4 0%,#6d5bd0 45%,#8b6bd8 100%);
    color:#fff;
    padding:52px 32px 46px;
  }
  .hero-inner{max-width:1180px;margin:0 auto}
  .hero .eyebrow{
    font-size:12.5px;letter-spacing:.14em;text-transform:uppercase;
    opacity:.86;font-weight:600;margin-bottom:14px;
  }
  .hero h1{margin:0 0 14px;font-size:clamp(26px,3.6vw,40px);line-height:1.25;font-weight:750;letter-spacing:-.01em}
  .hero p.lede{margin:0;max-width:820px;font-size:16px;opacity:.94;line-height:1.7}
  .hero .meta{margin-top:22px;display:flex;flex-wrap:wrap;gap:8px}
  .chip{
    display:inline-block;padding:5px 12px;border-radius:999px;font-size:12.5px;
    background:rgba(255,255,255,.16);border:1px solid rgba(255,255,255,.28);font-weight:500;
  }

  /* ---------- Layout ---------- */
  .wrap{max-width:1180px;margin:0 auto;padding:30px 32px 80px;display:grid;grid-template-columns:216px 1fr;gap:36px;align-items:start}
  @media (max-width:960px){.wrap{grid-template-columns:1fr;gap:20px;padding:24px 18px 60px}.hero{padding:36px 18px 32px}}

  /* ---------- TOC ---------- */
  nav.toc{position:sticky;top:24px;font-size:13.5px}
  nav.toc h4{margin:0 0 10px;font-size:11.5px;letter-spacing:.12em;text-transform:uppercase;color:var(--text-3);font-weight:700}
  nav.toc ol{list-style:none;margin:0;padding:0;counter-reset:toc}
  nav.toc li{counter-increment:toc;margin-bottom:2px}
  nav.toc a{
    display:block;padding:6px 10px 6px 30px;border-radius:7px;color:var(--text-2);
    text-decoration:none;position:relative;transition:.15s;
  }
  nav.toc a::before{
    content:counter(toc);position:absolute;left:9px;top:6px;font-size:11.5px;
    color:var(--text-3);font-variant-numeric:tabular-nums;
  }
  nav.toc a:hover{background:var(--surface);color:var(--brand)}
  @media (max-width:960px){nav.toc{display:none}}

  /* ---------- Sections ---------- */
  main{min-width:0}
  section{margin-bottom:38px;scroll-margin-top:20px}
  h2{
    font-size:21px;font-weight:720;margin:0 0 6px;padding-bottom:10px;
    border-bottom:2px solid var(--border);letter-spacing:-.005em;
  }
  h2 .num{color:var(--brand);margin-right:8px;font-variant-numeric:tabular-nums}
  h3{font-size:16.5px;font-weight:680;margin:26px 0 10px;color:#22262f}
  h4{font-size:14.5px;font-weight:660;margin:20px 0 8px;color:var(--text-2)}
  p{margin:0 0 12px}
  ul,ol{margin:0 0 14px;padding-left:22px}
  li{margin-bottom:6px}
  strong{font-weight:680;color:#101318}
  a{color:var(--brand)}

  /* ---------- Cards ---------- */
  .card{background:var(--surface);border:1px solid var(--border);border-radius:var(--radius);padding:20px 22px;margin-bottom:16px;box-shadow:var(--shadow)}
  .card > :last-child{margin-bottom:0}
  .card-title{font-weight:680;font-size:15.5px;margin:0 0 8px;display:flex;align-items:baseline;gap:10px}
  .card-title .idx{
    flex:none;width:22px;height:22px;border-radius:6px;background:var(--brand-soft);color:var(--brand);
    font-size:12.5px;font-weight:700;display:inline-flex;align-items:center;justify-content:center;
    transform:translateY(-1px);
  }

  /* ---------- Callouts ---------- */
  .callout{border-left:3px solid var(--brand);background:var(--brand-soft);border-radius:0 10px 10px 0;padding:14px 18px;margin:16px 0}
  .callout.warn{border-color:#d98b1f;background:var(--amber-soft)}
  .callout.danger{border-color:#d04a3a;background:var(--red-soft)}
  .callout.ok{border-color:#25916a;background:var(--green-soft)}
  .callout .lbl{display:block;font-size:12px;font-weight:700;letter-spacing:.08em;text-transform:uppercase;margin-bottom:6px;opacity:.75}
  .callout p:last-child{margin-bottom:0}

  /* ---------- Tables ---------- */
  .tw{overflow-x:auto;margin:14px 0 18px;border:1px solid var(--border);border-radius:var(--radius);background:var(--surface);box-shadow:var(--shadow)}
  table{border-collapse:collapse;width:100%;font-size:14px;min-width:520px}
  th{background:var(--surface-2);text-align:left;font-weight:660;padding:11px 14px;border-bottom:1px solid var(--border);color:#22262f;white-space:nowrap}
  td{padding:10px 14px;border-bottom:1px solid #eef0f5;vertical-align:top}
  tbody tr:last-child td{border-bottom:none}
  tbody tr:hover{background:#fafbfd}
  td.num{font-variant-numeric:tabular-nums;white-space:nowrap}

  /* ---------- Badges ---------- */
  .badge{display:inline-block;padding:2px 9px;border-radius:6px;font-size:12px;font-weight:650;line-height:1.6;white-space:nowrap}
  .b-red{background:var(--red-soft);color:var(--red)}
  .b-amber{background:var(--amber-soft);color:var(--amber)}
  .b-green{background:var(--green-soft);color:var(--green)}
  .b-brand{background:var(--brand-soft);color:var(--brand)}
  .b-cyan{background:var(--cyan-soft);color:var(--cyan)}
  .b-gray{background:var(--surface-2);color:var(--text-2)}

  /* ---------- Stats ---------- */
  .stats{display:grid;grid-template-columns:repeat(auto-fit,minmax(158px,1fr));gap:12px;margin:18px 0 22px}
  .stat{background:var(--surface);border:1px solid var(--border);border-radius:var(--radius);padding:15px 16px;box-shadow:var(--shadow)}
  .stat .v{font-size:22px;font-weight:740;letter-spacing:-.02em;color:var(--brand);font-variant-numeric:tabular-nums;line-height:1.25}
  .stat .k{font-size:12.5px;color:var(--text-3);margin-top:5px;line-height:1.5}
  .stat .note{font-size:11.5px;color:var(--text-3);margin-top:6px;opacity:.85}

  /* ---------- Mermaid ---------- */
  figure{margin:18px 0;background:var(--surface);border:1px solid var(--border);border-radius:var(--radius);padding:20px 18px 12px;box-shadow:var(--shadow);overflow-x:auto}
  figure .mermaid{display:flex;justify-content:center;min-height:60px}
  figcaption{font-size:12.5px;color:var(--text-3);text-align:center;padding:10px 6px 2px;border-top:1px dashed var(--border);margin-top:12px;line-height:1.6}

  /* ---------- Verdict ---------- */
  .verdict{
    background:linear-gradient(135deg,#1f2130 0%,#33365a 100%);color:#eef0fa;
    border-radius:var(--radius);padding:24px 26px;margin:20px 0;
  }
  .verdict h3{color:#fff;margin:0 0 12px;font-size:17px}
  .verdict p{margin:0 0 10px;font-size:15px;line-height:1.8}
  .verdict p:last-child{margin-bottom:0}
  .verdict strong{color:#fff}

  /* ---------- Footer ---------- */
  footer{border-top:1px solid var(--border);margin-top:10px;padding:22px 0 0;font-size:13px;color:var(--text-3);line-height:1.7}
  footer .src{margin-bottom:10px}
  footer ul{padding-left:20px;margin:8px 0}

  .kbd{background:var(--surface-2);border:1px solid var(--border);border-radius:5px;padding:1px 6px;font-size:12.5px}
  .hr{height:1px;background:var(--border);margin:34px 0}
</style>
</head>
<body>

<header class="hero">
  <div class="hero-inner">
    <div class="eyebrow">AI 前沿模型深度分析</div>
    <h1>Jev AI 决策模型深度分析报告</h1>
    <p class="lede">
      TypeSafe AI 于 2026-09-15 发布的首个 System One 模型。它不生成任何文本，只输出类型安全的决策值与校准概率——
      业界称之为「智能 if 语句」。本报告覆盖其定位、技术特点、应用场景、热度成因、优势局限、风险与前景。
    </p>
    <div class="meta">
      <span class="chip">内容截止 2026-09-22</span>
      <span class="chip">HTML 版 2026-09-23</span>
      <span class="chip">发布后第 7 天</span>
      <span class="chip">含 5 张架构图</span>
      <span class="chip">结论明确版</span>
    </div>
  </div>
</header>

<div class="wrap">

  <nav class="toc">
    <h4>目录</h4>
    <ol>
      <li><a href="#s0">核心结论</a></li>
      <li><a href="#s1">产品定位与功能</a></li>
      <li><a href="#s2">主要技术特点</a></li>
      <li><a href="#s3">典型应用场景</a></li>
      <li><a href="#s4">市场热度成因</a></li>
      <li><a href="#s5">优势与局限</a></li>
      <li><a href="#s6">潜在风险</a></li>
      <li><a href="#s7">发展前景</a></li>
      <li><a href="#s8">本地落地启示</a></li>
      <li><a href="#s9">附录</a></li>
    </ol>
  </nav>

  <main>

  <!-- ============ 0 ============ -->
  <section id="s0">
    <h2><span class="num">0</span>核心结论</h2>
    <p>
      <strong>一句话定义：</strong>Jev 不是「更强的 ChatGPT」，而是一种新的计算原语——可以理解为
      <strong>「智能 if 语句」</strong>。它接收非结构化状态 + 预先定义好的类型化问题，直接返回
      <strong>类型安全的决策值 + 校准过的置信概率</strong>。
    </p>

    <div class="stats">
      <div class="stat"><div class="v">193.6×</div><div class="k">官方宣称的提速倍数</div><div class="note">口径需打折，见 §5.2</div></div>
      <div class="stat"><div class="v">$0.042</div><div class="k">每百万输入 token 定价</div><div class="note">输出 token 完全免费</div></div>
      <div class="stat"><div class="v">70–500ms</div><div class="k">端到端延迟</div><div class="note">前沿 LLM 为 3–329 秒</div></div>
      <div class="stat"><div class="v">12.8%</div><div class="k">Vercel 付费团队 24h 内采用</div><div class="note">创该平台最快采用纪录</div></div>
    </div>

    <div class="card">
      <p class="card-title"><span class="idx">1</span>热度是真的，但热度不等于成熟</p>
      <p>
        创始人光环（RLHF 共同发明人 Diogo Almeida）+ 反直觉叙事（「哑巴 AI」「拒绝写一个字」）+
        极端性价比数字三者叠加，制造了 2026 年 9 月最强的开发者话题。Vercel AI Gateway 上线 24 小时内
        被近 <strong>13% 付费团队</strong>采用，是 GPT-5.6 的 2 倍、Fable 5.1 的 6 倍以上，创平台最快纪录。
      </p>
    </div>

    <div class="card">
      <p class="card-title"><span class="idx">2</span>需求侧的逻辑是成立的</p>
      <p>
        Agent 时代真正的成本痛点不在「生成」，而在「判断」——一次用户操作可能触发上百次分类、路由、校验、重试判断。
        用前沿大模型干这些事是杀鸡用牛刀。Vercel 数据佐证：2026 年 5 月其生产网关的 token 流量中
        <strong>58.9% 来自 Agent 类负载</strong>。Jev 精准踩在这个痛点上。
      </p>
    </div>

    <div class="card">
      <p class="card-title"><span class="idx">3</span>技术护城河目前唯一，但未必持久</p>
      <p>
        所有 LLM 都能做分类，但 Jev 是唯一把<strong>「诚实的概率」当作训练目标</strong>、并把
        <strong>类型安全交由架构保证</strong>（而非靠 JSON 修复循环）的公开模型。这让「置信度门控 + 无人值守自动化」
        第一次有了可依赖的依据。风险是：大厂只需在 function calling / JSON mode 上补齐校准能力，即可在生态内消化这个卖点。
      </p>
    </div>

    <div class="card">
      <p class="card-title"><span class="idx">4</span>最大的不确定性来自「不可验证」</p>
      <p>
        截至报告日，Jev 没有论文、没有公开 benchmark 成绩、架构与参数量与训练算力全部未公开，现有性能数字基本来自官方自建评测，
        官方自己也在博客里承认评测存在偏向。<strong>「零幻觉」应严格理解为「输出格式安全」，而不是「判断正确」。</strong>
      </p>
    </div>

    <div class="card">
      <p class="card-title"><span class="idx">5</span>正确的使用姿势</p>
      <p>
        「窄、闭合、高频、延迟敏感、错了代价可控」。把它当库调用、当代码里的一个函数，而不是当聊天窗口里的助手；
        不要用它做 LLM 裁判（judge），更不要用它做上下文压缩（compaction）——这两个当前最火的玩法在工程上都是类别错误。
      </p>
    </div>

    <div class="callout warn">
      <span class="lbl">给运维 / 平台工程的可操作建议</span>
      <p>
        Jev 适合做 CI/CD 门禁判断、构建日志归因分类、告警降噪与分级路由、Agent 工具调用安全审查。
        但<strong>目前服务未向中国大陆地区开放</strong>，且为纯托管 API、无自托管权重，数据出境与合规是落地前必须先解决的前提。
      </p>
    </div>
  </section>

  <div class="hr"></div>

  <!-- ============ 1 ============ -->
  <section id="s1">
    <h2><span class="num">1</span>产品定位与核心功能</h2>

    <h3>1.1 定位：面向机器，而非面向人</h3>
    <p>TypeSafe AI 的核心论点来自创始人 Diogo Almeida 的一句反问：</p>
    <div class="callout">
      <p>「我们已经把人类语言优化了四年，但语言对自动化没有用，因为计算机说的是另一种语言。」</p>
    </div>
    <p>
      在他看来，RLHF 的目标函数设定是当前 AI 的根本问题——训练回路的目标是「取悦人类评委的主观偏好」，
      于是模型被系统性地激励去<strong>过度承诺、虚假迎合</strong>，而不是追求客观准确。幻觉不是预训练的产物，
      而是后训练阶段奖励不对称性的内生结果：<strong>坦承「不知道」会被算法惩罚，自信地编造反而拿高分。</strong>
    </p>
    <p>
      因此 TypeSafe 走了一条相反的路：不做「更会聊天」的模型，而做<strong>原生供机器调用的模型</strong>。
      产品对外定位为「<strong>前沿智能的函数调用</strong>」（frontier-intelligence function call）——非结构化状态进，
      类型化概率决策出。
    </p>

    <div class="tw">
      <table>
        <thead><tr><th>维度</th><th>传统 LLM</th><th>Jev</th></tr></thead>
        <tbody>
          <tr><td>优化目标</td><td>人类偏好（RLHF）/ 可验证奖励（RLVR）</td><td><strong>校准决策（RLCD）</strong></td></tr>
          <tr><td>输入重心</td><td>顺序消息（对话历史）</td><td>结构化程序状态</td></tr>
          <tr><td>输出形态</td><td>字符串，需解析 + 校验</td><td>预先定义的类型安全值</td></tr>
          <tr><td>采样方式</td><td>自回归，逐 token 生成</td><td><strong>并行</strong>，一次查询产出全部输出</td></tr>
          <tr><td>置信度</td><td>普遍过度自信、不一致</td><td>每个输出都带校准概率</td></tr>
          <tr><td>主要消费方</td><td>人（聊天、Copilot、编码 Agent）</td><td>软件（工作流、路由、门禁）</td></tr>
        </tbody>
      </table>
    </div>

    <h3>1.2 核心功能：只做三件事</h3>
    <p>
      Jev 的 API 形态极其收窄。一次请求 = <strong>非结构化状态</strong>（JSON、文本、日志、页面内容、棋盘局面等）+
      <strong>一组类型化问题</strong>；每个问题预先声明允许的答案格式。返回值是三类原语之一：
    </p>
    <div class="tw">
      <table>
        <thead><tr><th>决策原语</th><th>语义</th><th>典型用途</th></tr></thead>
        <tbody>
          <tr><td><span class="badge b-brand">Choice</span></td><td>从预设枚举中选一个</td><td>工单分派部门、工具选择、路由目标</td></tr>
          <tr><td><span class="badge b-cyan">Score</span></td><td>打分</td><td>风险评分、质量打分、优先级排序</td></tr>
          <tr><td><span class="badge b-green">Yes / No（Noul）</span></td><td>布尔式概率判定，返回 0–1 的真实性概率</td><td>是否重试、是否放行、是否越狱、真伪判断</td></tr>
        </tbody>
      </table>
    </div>
    <p>三个工程特性值得单独强调：</p>
    <ol>
      <li><strong>一次请求可并行提出多个独立问题</strong>，一个问题不影响另一个，且不用为每个问题付一次完整的自回归生成代价。</li>
      <li><strong>输出带校准概率与置信度分数</strong>，应用代码可以直接设阈值决定「自动执行」还是「升级人工」。</li>
      <li><strong>无自由字符串、无 JSON 修复循环</strong>——官方称类型错误在数学上不可能出现。</li>
    </ol>

    <h3>1.3 关键事实速查</h3>
    <div class="tw">
      <table>
        <tbody>
          <tr><th>发布方</th><td>TypeSafe AI（旧金山），结束两年隐身期</td></tr>
          <tr><th>创始人</th><td>Diogo Almeida（前 OpenAI、RLHF 共同发明人）、Erik Gafni、Sasha Sheng</td></tr>
          <tr><th>融资</th><td>4000 万美元种子轮，DCVC 领投（据报道投后估值约 2 亿美元）</td></tr>
          <tr><th>首次发布</th><td>2026-09-15，早鸟访问 + 候补名单</td></tr>
          <tr><th>全面开放</th><td>2026-09-21 取消候补名单，5 美元额度起（约 1.2 亿 token）</td></tr>
          <tr><th>定价</th><td class="num">输入 $0.042 / 百万 token（$42 / 十亿），输出完全免费</td></tr>
          <tr><th>延迟</th><td class="num">端到端 70–500 毫秒</td></tr>
          <tr><th>上下文窗口</th><td class="num">32K token</td></tr>
          <tr><th>模态</th><td>纯文本（暂不支持图像输入）</td></tr>
          <tr><th>部署方式</th><td>仅托管 API，未发布权重，无自托管选项</td></tr>
          <tr><th>名称由来</th><td>经济学家 William Stanley Jevons 的「杰文斯悖论」；类别名来自 Kahneman 的 System 1 / System 2</td></tr>
          <tr><th>中国大陆</th><td><span class="badge b-red">服务未向中国大陆地区开放</span></td></tr>
        </tbody>
      </table>
    </div>
    <p>
      命名逻辑值得记一笔：杰文斯悖论指出蒸汽机效率提升反而让煤炭消耗暴涨。TypeSafe 押的是同一件事——
      <strong>智能单位成本每下降一个数量级，就会解锁数量级更多的用例</strong>。
    </p>
  </section>

  <div class="hr"></div>

  <!-- ============ 2 ============ -->
  <section id="s2">
    <h2><span class="num">2</span>主要技术特点</h2>

    <h3>2.1 放弃自回归：并行采样器</h3>
    <p>
      传统 LLM 逐 token 生成，每个新 token 依赖前一个，因此输出越长，延迟、算力和出错窗口同步膨胀。
      Jev 采用<strong>并行采样</strong>：对同一份状态一次性算出全部输出。
    </p>
    <p>
      这是它能同时做到「低延迟 + 批量」的结构性原因，也是「输出免费」定价得以成立的底气——
      输出不再是一个需要计量的长尾成本。
    </p>

    <figure>
      <pre class="mermaid">
flowchart LR
    S["非结构化状态 state"] --> J["Jev 决策模型"]
    Q["类型化问题 questions"] --> J
    J --> O["类型安全决策值 + 校准概率"]
    O --> G{"置信度是否越过阈值"}
    G -->|"是"| ACT["代码自动执行下一步"]
    G -->|"否"| ESC["升级人工审核或转交大模型"]
      </pre>
      <figcaption>图 1 ｜ Jev 调用范式。它替换的是「LLM 生成一段 JSON 再解析」的中间环节，输出直接进入代码分支，阈值由工程师设定。</figcaption>
    </figure>

    <h3>2.2 RLCD：第三种训练范式</h3>
    <p>
      Jev 用官方称为 <strong>Reinforcement Learning for Calibrated Decisions（RLCD，校准决策强化学习）</strong>
      的方法训练，且<strong>完全使用合成数据</strong>。三条路线的对比是理解 Jev 的关键：
    </p>
    <div class="tw">
      <table>
        <thead><tr><th>路线</th><th>优化目标</th><th>产出接口</th><th>典型代表</th></tr></thead>
        <tbody>
          <tr><td>RLHF</td><td>人类主观偏好</td><td>自由文本对话</td><td>ChatGPT、Claude</td></tr>
          <tr><td>RLVR</td><td>封闭数理问题的绝对正确性</td><td>带思维链的推理</td><td>o 系列、DeepSeek-R1 类</td></tr>
          <tr><td><strong>RLCD</strong></td><td>概率与现实相符的<strong>校准决策</strong></td><td>机器原生结构化决策</td><td><strong>Jev</strong></td></tr>
        </tbody>
      </table>
    </div>
    <p>
      校准（calibration）的含义是：如果 Jev 对一百个输入说 0.9，那么大约九十次应该为真。
      RLHF 优化「人喜欢什么答案」，RLCD 优化「概率是否诚实」。这是 Jev 与「给 LLM 加 JSON mode」
      在<strong>目标函数层面</strong>的本质差别。
    </p>

    <h3>2.3 类型安全由架构保证</h3>
    <p>这是 Jev 最容易被误读的一点，必须精确理解：</p>

    <figure>
      <pre class="mermaid">
flowchart TD
    A["开发者预先定义可选答案集合"] --> B["Jev 只能从集合内选择"]
    B --> C["不可能输出集合外的值"]
    B --> D["但仍可能从集合内选错"]
    C --> E["类型安全成立：不会产生格式错误"]
    D --> F["事实正确性：不受任何保证"]
      </pre>
      <figcaption>图 2 ｜ 「零幻觉」的真实边界。官方给幻觉率写 0% 时，脚注明确说明这不是实测值——schema 匹配是架构保证的，所以直接写 0。</figcaption>
    </figure>

    <div class="callout danger">
      <span class="lbl">关键澄清</span>
      <p>
        它保证的是「不会输出猫狗鸟之外的答案」，<strong>不保证「不会把猫答成狗」</strong>。
        Hacker News 上获赞最多的评论把这件事说得最直白：<strong>它不可能产生非法类型，但仍然可能产生一个合法的错误值。</strong>
        请把 "type-safe" 理解为输出格式的属性，而非准确率的承诺。
      </p>
    </div>

    <h3>2.4 性能与成本宣称（需打折看待）</h3>
    <p>官方主页数字：<strong>快 193.6 倍、便宜 444.6 倍</strong>。但这些数字的构造方式必须看清楚：</p>
    <ul>
      <li>来自 TypeSafe 自建的 <strong>4 个工作流评测</strong>，参考答案取 <strong>GPT-6 Astra 与 Fable 5.1 的平均值</strong>——这个口径天然偏向 OpenAI 与 Anthropic 的模型（官方自己也承认这一点）。</li>
      <li>工作流由 TypeSafe <strong>模型能力团队成员</strong>制作，官方承认「可能存在偏向」。</li>
      <li>官方原话：「我们预期这些是真实世界收益中<strong>偏高端</strong>的取值。」</li>
      <li>演示用的是简短高密度输入，官方承认「这给我们的模型带来优势」。</li>
    </ul>
    <p>
      同价位对照（官方口径）：GPT-5.6 Terra 输入 $2 / 百万 token、输出 $12 / 百万 token，而 Jev 输出免费。
      官网演示中 Jev <span class="mono">0.114s</span> 完成，对比 GPT-5.6 Terra 的 <span class="mono">8.566s</span>。
    </p>
  </section>

  <div class="hr"></div>

  <!-- ============ 3 ============ -->
  <section id="s3">
    <h2><span class="num">3</span>典型应用场景</h2>

    <h3>3.1 官方定位的用例族</h3>
    <ul>
      <li><strong>AI 驱动的智能工作流 / 智能 if 语句</strong>：凡手写规则太脆、上大模型又太贵的判断点——分类、路由、打分、抽取、分支。</li>
      <li><strong>大数据 map-reduce 特征化</strong>：把海量原始数据转成特征与洞察。</li>
      <li><strong>实时应用</strong>：100 毫秒级延迟意味着可以放进请求链路甚至游戏循环。</li>
      <li><strong>校验一切</strong>：给 LLM 的 prompt、推理轨迹、输出做打分、裁判、护栏、越狱检测。</li>
      <li><strong>Agent 工具调用与模型编排。</strong></li>
    </ul>

    <h3>3.2 已有第三方实践（早期）</h3>
    <div class="tw">
      <table>
        <thead><tr><th>采用方</th><th>场景</th><th>结果</th></tr></thead>
        <tbody>
          <tr><td>Vercel</td><td>替换 ChatGPT Luna 5.6 做命令安全审查</td><td>快 <strong>5–18 倍</strong>，该任务准确率更高</td></tr>
          <tr><td>Bryo AI</td><td>商务邮件分类，对比 Gemini</td><td>便宜 <strong>10–20 倍</strong>，Gemini 准确率略高</td></tr>
          <tr><td>Every（Mike Taylor）</td><td>37 篇文档 × 21 问题 = 777 次判断</td><td><strong>0.7 秒</strong>内完成，成本约 <strong>0.25 美分</strong></td></tr>
          <tr><td>Every（Dan Shipper）</td><td>12 篇文本 × 4 项写作检查，对比 Fable 5.1</td><td>中位 <strong>0.35s</strong>/篇 vs 8.83s；成本低约 <strong>580 倍</strong>；植入缺陷 7 个抓到 6 个，Fable 全抓到</td></tr>
          <tr><td>社区（r/PiCodingAgent）</td><td>编码 Agent 护栏：拦截破坏性调用、按项目规则文件审查代码、识别卡死循环、抓「已完成但没跑测试」的虚假声明</td><td><span class="badge b-gray">早期实验</span></td></tr>
          <tr><td>社区</td><td>工具调用安全评分 + 按任务难度选模型的路由器</td><td><span class="badge b-gray">早期实验</span></td></tr>
          <tr><td>社区</td><td>邮件分诊：300 封邮件分入 15 个部门</td><td><strong>9.9 秒</strong>，花费 <strong>$0.036</strong>（同期 DeepSeek 只读完 10 封）</td></tr>
        </tbody>
      </table>
    </div>

    <h3>3.3 官方演示</h3>
    <ul>
      <li><strong>Doom</strong>：Jev 基于结构化的游戏状态数据（不是图像）实时决策，约每秒 10 次查询，成本约 <strong>$7 / 小时</strong>。官方明确说明这是「结构化状态文本，还不是图像」。</li>
      <li><strong>Wikiracing</strong>：在维基百科链接间寻路，单步可选基数高达数百至上千，Jev 支持上限 <strong>255</strong>；超过时采用「先独立打分、再显式选择」的两阶段策略。</li>
    </ul>

    <h3>3.4 中国侧生态动作</h3>
    <p>
      2026-09-19，中国人工智能企业 APUS（麒麟合盛网络技术股份有限公司）AI 实验室公布了
      <strong>全球最早一批针对 Jev 的独立开源复现成果</strong>，包装为开箱即用的 Agent Skill
      <span class="mono">fast-browser-use</span>：
    </p>
    <ul>
      <li>基于 Jev 公开文档展示的输入范式与评估逻辑，分析其「<strong>跳过自回归解码、隐状态直接打分</strong>」的核心逻辑。</li>
      <li>复现了单 Token Logits 快速决策、Jev KV-Cache 广播与并发批量评估机制。</li>
      <li>支持 macOS / Linux / Windows，<strong>GPU 服务器与无 GPU 的 Mac/PC 均可运行</strong>，可纯本地离线执行。</li>
      <li>全部代码以 <strong>MIT 协议</strong>开放。</li>
    </ul>
    <div class="callout warn">
      <span class="lbl">注意区分</span>
      <p><strong>开源的是复现项目，不是 Jev 本身。</strong>Jev 本体至今未开源、未发布权重。</p>
    </div>
  </section>

  <div class="hr"></div>

  <!-- ============ 4 ============ -->
  <section id="s4">
    <h2><span class="num">4</span>市场热度成因</h2>

    <h3>4.1 关键时间线</h3>
    <figure>
      <pre class="mermaid">
timeline
    title Jev 关键时间线（2026 年）
    9月14-15日 : TypeSafe 结束两年隐身期 : 发布首个 System One 模型 Jev，开启早鸟访问与候补名单
    9月15-18日 : API 因流量激增一度无法服务用户 : Vercel、Cloudflare、LangChain、Langfuse 相继接入
    9月19日 : APUS 公布开源复现 fast-browser-use : 国内技术社区跟进讨论
    9月21日 : 取消候补名单 : 5 美元额度起用，约等于 1.2 亿 token
      </pre>
      <figcaption>图 3 ｜ 从发布到全面开放仅用 6 天，生态接入速度快于独立验证速度——这是本轮热度最重要的特征。</figcaption>
    </figure>

    <h3>4.2 热度为什么这么高——六个叠加因素</h3>

    <div class="card">
      <p class="card-title"><span class="idx">1</span>创始人光环</p>
      <p>Diogo Almeida 是 RLHF 的共同发明人、ChatGPT 背后研究的参与者之一。他公开说「RLHF 是一条弯路」，本身就是极具传播力的话题。</p>
    </div>

    <div class="card">
      <p class="card-title"><span class="idx">2</span>反直觉叙事</p>
      <p>在所有人都在比「谁的模型更会说话」时，一个<strong>故意不写字</strong>的模型天然具备新闻性。「哑巴 AI」「它拒绝写一个字」成为媒体标题的默认句式。</p>
    </div>

    <div class="card">
      <p class="card-title"><span class="idx">3</span>数字足够极端</p>
      <p>「快 200 倍、便宜 400 倍」这类量级对比在社交传播中极易扩散，即便其口径需要打折。</p>
    </div>

    <div class="card">
      <p class="card-title"><span class="idx">4</span>生态在 36 小时内接住了它</p>
      <p>
        Vercel AI Gateway 上线 Jev 后，24 小时内近 <strong>13% 的付费团队</strong>使用，是 GPT-5.6 的 2 倍、Fable 5.1 的 6 倍以上，
        创平台最快采用纪录；Cloudflare、LangChain、Langfuse 迅速加入；Vercel 的 AI SDK 7 甚至加了对应的 evaluate 方法。
        <strong>生态先于验证，这是本轮热度最重要的放大器。</strong>
      </p>
    </div>

    <div class="card">
      <p class="card-title"><span class="idx">5</span>稀缺感与「打挂的 API」</p>
      <p>
        早期只开放候补名单，需求一度把官方 API 打到无法服务用户；有报道称候补队伍一度达到十万量级。
        发布视频两天内播放约 <strong>3600 万次</strong>，Hacker News 讨论帖两天内冲到 <strong>1821 分、480 条评论</strong>，
        创始人以 "CompleteSkeptic" 的 ID 亲自下场答疑。
      </p>
    </div>

    <div class="card">
      <p class="card-title"><span class="idx">6</span>缺失论文反而推高了热度</p>
      <p>
        由于 Jev 不开源、无论文，社区在两天内自行涌现出<strong>至少 6 个克隆项目</strong>，技术路线横跨 ModernBERT 编码器、
        扩散模型、LoRA 微调、NLI 分类器头、轻量级选项注意力等方向。大量推测、复现尝试与「酸葡萄式批评」共同构成了话题燃料。
      </p>
    </div>
  </section>

  <div class="hr"></div>

  <!-- ============ 5 ============ -->
  <section id="s5">
    <h2><span class="num">5</span>优势与局限</h2>

    <h3>5.1 结构性优势</h3>
    <div class="tw">
      <table>
        <thead><tr><th>优势</th><th>说明</th></tr></thead>
        <tbody>
          <tr><td><span class="badge b-green">延迟</span></td><td>70–500 毫秒，可以坐在请求链路内部或游戏循环里，这是大模型结构上做不到的</td></tr>
          <tr><td><span class="badge b-green">成本</span></td><td>100 万次调用（每次 1000 输入 token）约 $42；输出免费意味着可以「检查每一次判断」而不是抽样</td></tr>
          <tr><td><span class="badge b-green">类型安全</span></td><td>架构保证零格式错误、零 JSON 修复循环、零重试</td></tr>
          <tr><td><span class="badge b-green">诚实概率</span></td><td>唯一把校准作为训练目标的公开模型，让置信度门控和无人值守自动化有据可依</td></tr>
          <tr><td><span class="badge b-green">批量并行</span></td><td>一次请求回答多个独立问题，互不污染</td></tr>
          <tr><td><span class="badge b-green">定位清晰</span></td><td>不与 ChatGPT/Claude 正面竞争，而是做它们下面的决策层</td></tr>
        </tbody>
      </table>
    </div>

    <h3>5.2 已知局限（按严重度排序）</h3>

    <h4><span class="badge b-red">一级</span> 影响可用性的硬约束</h4>
    <div class="tw">
      <table>
        <thead><tr><th style="width:38px">#</th><th>局限</th><th>说明</th></tr></thead>
        <tbody>
          <tr><td class="num">1</td><td><strong>中文 / CJK 能力弱</strong></td><td>官方文档主动标注 CJK 准确率偏低；实测者反馈中文结果不稳定，英文短文本才稳定。<strong>对中文场景这是直接的门槛。</strong></td></tr>
          <tr><td class="num">2</td><td><strong>无推理能力</strong></td><td>在 Browser Use 创始人的长程浏览器任务测试中，Jev 得分 <strong>1/20</strong>，而具备推理能力的 Luna 为 <strong>17/20</strong>。它不做多跳推理、不做带间接性的任务。</td></tr>
          <tr><td class="num">3</td><td>数学、计数、日期运算弱</td><td>官方口径「顶多 GPT-4 水平」。</td></tr>
          <tr><td class="num">4</td><td>32K 上下文、纯文本</td><td>超长文档（整本书、超长日志）力不从心；不支持图像输入。有测试指出非英语场景 token 效率差（挪威语约 2.06 字符/token）。</td></tr>
        </tbody>
      </table>
    </div>

    <h4><span class="badge b-amber">二级</span> 影响可信度的证据缺口</h4>
    <div class="tw">
      <table>
        <thead><tr><th style="width:38px">#</th><th>局限</th><th>说明</th></tr></thead>
        <tbody>
          <tr><td class="num">5</td><td><strong>「零幻觉」要打折</strong></td><td>类型正确 ≠ 事实正确。CEO 本人确认它可能自信地选错选项。</td></tr>
          <tr><td class="num">6</td><td><strong>无技术论文、无公开 benchmark</strong></td><td>RLCD 仅有博客描述，arXiv 无预印本，架构、参数量、训练算力全部未公开，外部<strong>无法验证「校准」这一核心声称</strong>。官方明确不发布公共 benchmark 成绩（理由是防止 benchmaxxing）。</td></tr>
          <tr><td class="num">7</td><td>准确率并不领先</td><td>据第三方整理，Jev 在官方四工作流基准上准确率约 <strong>68%</strong>，与中端对话模型相当，明显低于前沿推理系统。</td></tr>
        </tbody>
      </table>
    </div>

    <h4><span class="badge b-amber">三级</span> 工程与使用层面的坑</h4>
    <div class="tw">
      <table>
        <thead><tr><th style="width:38px">#</th><th>局限</th><th>说明</th></tr></thead>
        <tbody>
          <tr><td class="num">8</td><td>无法输出理由</td><td>只给最终选择与数字，没有 rationale，审计和人工复核场景基本不可用。</td></tr>
          <tr><td class="num">9</td><td>不支持动态工具调用参数</td><td>行号、生成代码、自定义查询等均不支持。</td></tr>
          <tr><td class="num">10</td><td>校准不是一次性的</td><td>校准只在<strong>你自己的流量分布</strong>上统计成立，输入分布一变就必须重新测量。</td></tr>
          <tr><td class="num">11</td><td>提问方式会显著影响校准</td><td>独立评测者 Lindfors 发现：措辞越谨慎，校准反而越差，ECE 从 <strong>0.040 恶化到 0.116</strong>。写问题是门手艺。</td></tr>
          <tr><td class="num">12</td><td>演示环境 ≠ 真实环境</td><td>精修 demo 跑在干净沙箱里；面对混乱 DOM、异步加载、弹窗的真实网页，Jev 会失效。</td></tr>
        </tbody>
      </table>
    </div>

    <h4><span class="badge b-red">四级</span> 经济与策略层面</h4>
    <div class="tw">
      <table>
        <thead><tr><th style="width:38px">#</th><th>局限</th><th>说明</th></tr></thead>
        <tbody>
          <tr><td class="num">13</td><td>定价可持续性未知</td><td>官方坦言「无法证明它没有被补贴」，同时预期价格只会降不会涨。</td></tr>
          <tr><td class="num">14</td><td><strong>典型误用</strong></td><td>把 Jev 当 LLM 裁判（judge）或上下文压缩器（compaction）是当前最热的两个玩法，也是工程上最可疑的两个——前者需要「读懂代码库、扩展上下文、综合微妙差异」的能力，后者本质是<strong>综合摘要</strong>而非<strong>过滤</strong>，且编辑早期历史会使下游 cache 写入失效、反而更贵。</td></tr>
        </tbody>
      </table>
    </div>

    <h3>5.3 优势与局限的边界（一图看清）</h3>
    <figure>
      <pre class="mermaid">
flowchart TD
    ROOT["任务形态判断"] --> W["Jev 占优"]
    ROOT --> L["仍需大模型"]
    W --> W1["需要低延迟，坐在请求链路内"]
    W --> W2["需要海量重复判断，成本敏感"]
    W --> W3["需要阈值化置信度做门禁"]
    W --> W4["答案空间闭合、可预先枚举"]
    L --> L1["需要生成文本、代码或解释"]
    L --> L2["多跳推理与间接性任务"]
    L --> L3["算术、计数、日期运算"]
    L --> L4["低频但单次错误代价极高"]
      </pre>
      <figcaption>图 4 ｜ 能力边界判定。一句话判据：如果我写得出这个判断的选项列表，且错了还能兜住，就适合 Jev；如果需要它给我讲明白为什么，就不适合。</figcaption>
    </figure>
  </section>

  <div class="hr"></div>

  <!-- ============ 6 ============ -->
  <section id="s6">
    <h2><span class="num">6</span>潜在风险</h2>

    <h3>6.1 技术风险</h3>
    <ul>
      <li><strong>护城河可复制性。</strong>核心卖点是「校准 + 类型安全」。类型安全已可通过 JSON mode / schema 约束逼近；校准能力大厂补齐只是时间问题。一旦 OpenAI 的 function calling、Anthropic 的 tool use、Cohere 的路由 API 加上校准概率，Jev 的差异化会被大幅压缩。</li>
      <li><strong>竞品已在跟。</strong>Google 用 DiffusionGemma 演示的正是「补上视觉」的方向；BAML 这类「在生成模型上加一层格式修复」的方案虽然思路相反（检查错误 vs 设计消除错误），但能覆盖部分场景。</li>
      <li><strong>托管单点。</strong>只有托管 API，无权重、无自托管。对延迟敏感、数据敏感或有离线要求的场景直接出局（国内 APUS 复现项目正是钻这个空子）。</li>
    </ul>

    <h3>6.2 商业风险</h3>
    <ul>
      <li><strong>定价不可持续。</strong>输出免费是促销还是常态，官方自己也无法证明。</li>
      <li><strong>niche 是否够大。</strong>这是 TypeSafe 真正要回答的问题：<strong>当一个专门的决策模型品类，能否撑起一家独立模型公司？</strong></li>
      <li><strong>初期采用 ≠ 生产留存。</strong>Vercel 的 13% 衡量的是<strong>首周尝鲜</strong>，不代表长期生产使用。这是热度报告里最容易被误读的数字。</li>
    </ul>

    <h3>6.3 信任与复现风险</h3>
    <ul>
      <li>无论文、无独立基准、无架构披露，外部研究者无法复现「校准」声称；克隆项目中也有人质疑 RLCD 的所谓创新可能并不独特。</li>
      <li>官方自定义的评测口径（自建工作流、参考答案为两家友商模型的均值）存在系统性偏向，官方对此有披露但未提供替代方案。</li>
    </ul>

    <h3>6.4 合规与数据风险（对中国用户尤其关键）</h3>
    <ul>
      <li>服务<strong>未向中国大陆地区开放</strong>。</li>
      <li>纯托管 API 意味着输入数据出境，对涉及生产日志、代码、客户信息的场景是硬性合规障碍。</li>
      <li>目前唯一可行的本地化路径是 APUS 的 MIT 开源复现（<span class="mono">fast-browser-use</span>），但它复现的是<strong>范式与 Browser-use 场景</strong>，不是 Jev 模型本身，能力边界需要独立评估。</li>
    </ul>

    <h3>6.5 误用风险</h3>
    <div class="callout danger">
      <span class="lbl">最危险的一种失败模式</span>
      <p>
        最危险的不是它算错，而是<strong>它算错时仍然给出高置信度，而使用者误以为「零幻觉」等于「正确」</strong>。
        把非推理模型塞进需要推理的决策位，会得到一个「自信的、格式完美的错误答案」——
        这比一段明显的胡说八道更难被人工发现。
      </p>
    </div>
  </section>

  <div class="hr"></div>

  <!-- ============ 7 ============ -->
  <section id="s7">
    <h2><span class="num">7</span>发展前景</h2>

    <h3>7.1 判断一：Agent 的「快慢分工」会成为主流架构</h3>
    <p>
      Jev 验证的架构命题比 Jev 本身更重要。在一个 100 次模型调用的复杂 Agent 任务里，大约
      <strong>90 次是路由、状态检查、验证</strong>，只有约 10 次是真正的规划与生成。合理设计是把前者交给轻量决策模型，
      后者留给前沿大模型。<strong>负责组装与调度这套分工的工程层（业界称为 Harness）会成为真正的价值资产。</strong>
    </p>

    <figure>
      <pre class="mermaid">
flowchart TD
    REQ["用户请求 / 长程任务"] --> H["Harness 编排层：决定谁来做这一步"]
    H -->|"规划、生成、深度推理"| S2["System 2：前沿大模型"]
    H -->|"分类、路由、校验、重试判断"| S1["System 1：Jev 等决策模型"]
    S2 --> H
    S1 --> H
    H --> OUT["执行结果 / 下一步动作"]
      </pre>
      <figcaption>图 5 ｜ Jev 不是要取代大模型，而是把大模型从 90% 的琐碎判断中解放出来。这也是判断 Jev 长期价值时最该盯住的架构趋势。</figcaption>
    </figure>

    <h3>7.2 判断二：「决策模型」会成为一个独立品类</h3>
    <p>
      Earendil 的 CTO Armin Ronacher 的判断是：Jev 让<strong>低成本实时模型路由</strong>第一次变得可行，
      而一旦 TypeSafe 证明了非语言 Transformer 的实用价值，<strong>竞品概率模型会随之涌现</strong>。
      官方也在暗示将扩展到新的模态、并公布更多技术细节与评测。
    </p>

    <h3>7.3 判断三：近期看 Coding，中期看多模态</h3>
    <p>
      Almeida 在 HN 问答中提到，编码场景的难点是<strong>状态工程</strong>（把正确的依赖放进上下文），
      并确认编码主题的发布即将到来；TypeSafe 团队成员补充，近期编码方向的收益点在<strong>上下文管理与针对 AGENTS.md 的语义 lint</strong>。
      中长期官方明确表示会构建新模态的模型版本。
    </p>

    <h3>7.4 判断四：短期热度已经开始退潮，接下来看三件事</h3>
    <p>热度曲线必然回落（「API 被打挂」式的稀缺叙事不可持续）。真正决定它命运的是三个可验证指标：</p>
    <ol>
      <li><strong>留存，而非首周采用。</strong>Vercel 上那 13% 的付费团队，三个月后还有多少在跑生产流量？</li>
      <li><strong>独立验证。</strong>是否出现第三方基准、外部复现的校准测量，或至少一份技术论文。</li>
      <li><strong>价格与开放策略。</strong>输出免费是否延续？是否发布权重或自托管方案？这两点决定它能否进入数据敏感场景。</li>
    </ol>

    <div class="verdict">
      <h3>最终判断</h3>
      <p><strong>Jev 大概率会作为「决策层」活下来，但未必是以 TypeSafe 一家公司的形式。</strong></p>
      <p>
        它证明的品类（机器原生决策模型）需求真实、方向正确；它自己的位置（能否成为标准组件而非被大厂 SDK 吸收）尚不确定。
        对使用者而言，<strong>现在值得花两天做一次小规模验证，但不值得把它写进关键路径的强依赖。</strong>
      </p>
    </div>
  </section>

  <div class="hr"></div>

  <!-- ============ 8 ============ -->
  <section id="s8">
    <h2><span class="num">8</span>本地落地启示（运维 / 平台工程视角）</h2>
    <p>
      结合本地已有的 Jenkins、Prometheus、Grafana、Tekton 与 Docker Desktop K8s 环境，
      Jev 这类决策模型最可能落地在以下几个点：
    </p>
    <div class="tw">
      <table>
        <thead><tr><th>场景</th><th>用 Jev 做什么</th><th>当前可用的替代方案</th></tr></thead>
        <tbody>
          <tr>
            <td><strong>构建失败归因</strong></td>
            <td>把构建日志片段分类为「依赖下载失败 / 编译错误 / 测试失败 / 环境问题 / 需人工介入」</td>
            <td>规则匹配 + 关键词库；本机可跑轻量分类模型</td>
          </tr>
          <tr>
            <td><strong>告警降噪与分级</strong></td>
            <td>对 Prometheus 告警做「是否值得半夜叫人 / 归属哪个团队」的概率判定</td>
            <td>Grafana 告警规则 + 静默规则</td>
          </tr>
          <tr>
            <td><strong>Agent / Pipeline 工具调用门禁</strong></td>
            <td>判断某个命令或操作是否安全、是否破坏性、是否需审批</td>
            <td>白名单 + 人工审批</td>
          </tr>
          <tr>
            <td><strong>变更风险评估</strong></td>
            <td>对 MR / 提交做风险评分，决定是否跳过全量回归</td>
            <td>规则 + 历史统计</td>
          </tr>
          <tr>
            <td><strong>日志异常判定</strong></td>
            <td>对海量日志行做「正常 / 异常」的批量判断，替代正则堆砌</td>
            <td>Loki / Grafana + 正则</td>
          </tr>
        </tbody>
      </table>
    </div>

    <h3>落地前必须跨过的三道门槛</h3>
    <div class="card">
      <p class="card-title"><span class="idx">1</span>合规与数据边界</p>
      <p>
        服务未向中国大陆开放且为托管 API，生产日志与代码出境通常不可接受。可行路径是等权重开放，
        或采用本地可离线运行的复现方案（如 APUS 的 MIT 项目），<strong>但本地复现项目的能力边界必须先独立评测</strong>。
      </p>
    </div>
    <div class="card">
      <p class="card-title"><span class="idx">2</span>中文与 CJK 短板</p>
      <p>若输入含大量中文（工单、提交信息、中文日志），必须先做小样本验证，不能假设英文测试结论可平移。</p>
    </div>
    <div class="card">
      <p class="card-title"><span class="idx">3</span>不要放进强依赖</p>
      <p>定价策略、无权重、无 SLA 承诺都还在早期。适合做「旁路增强」，不适合做「阻断式门禁」的唯一判据。</p>
    </div>
  </section>

  <div class="hr"></div>

  <!-- ============ 9 ============ -->
  <section id="s9">
    <h2><span class="num">9</span>附录</h2>

    <h3>附录 A：关键数字与口径对照表</h3>
    <div class="tw">
      <table>
        <thead><tr><th>指标</th><th>官方宣称</th><th>需要注意的口径</th></tr></thead>
        <tbody>
          <tr><td>速度</td><td class="num">快 193.6 倍</td><td>来自官方自建 4 工作流评测，官方称属「偏高端」取值</td></tr>
          <tr><td>成本</td><td class="num">便宜 444.6 倍</td><td>同上；参考答案为 GPT-6 Astra 与 Fable 5.1 的均值</td></tr>
          <tr><td>延迟</td><td class="num">70–500 毫秒</td><td>对照前沿 LLM 的 3–329 秒；社区认为口径不完全对等</td></tr>
          <tr><td>幻觉率</td><td class="num">0%</td><td><strong>非实测值</strong>，来自 schema 匹配的架构保证</td></tr>
          <tr><td>准确率</td><td>未公开</td><td>第三方整理约 68%，低于前沿推理模型</td></tr>
          <tr><td>类型错误率</td><td class="num">0%</td><td>唯一可用单个反例证伪的硬声称</td></tr>
          <tr><td>上下文</td><td class="num">32K</td><td>不支持图像输入</td></tr>
          <tr><td>定价</td><td class="num">输入 $0.042/百万，输出免费</td><td>官方承认无法证明未补贴，预期只会降</td></tr>
          <tr><td>采用率</td><td class="num">Vercel 24 小时内近 13% 付费团队</td><td>衡量的是首周尝鲜，非生产留存</td></tr>
        </tbody>
      </table>
    </div>

    <h3>附录 B：30 秒决策法</h3>
    <p>对你的任务依次问：</p>
    <ol>
      <li>答案空间能预先枚举吗？——不能，不要用。</li>
      <li>需要它解释理由吗？——需要，不要用。</li>
      <li>需要多跳推理 / 算术 / 日期计算吗？——需要，不要用。</li>
      <li>输入里中文占比高吗？——高，先做小样本验证。</li>
      <li>单次判断错误的代价可控吗？——不可控，加人工门禁或换模型。</li>
      <li>数据出境合规吗？——不合规，等自托管方案。</li>
      <li>以上全过——<strong>值得试，而且通常很划算。</strong></li>
    </ol>

    <h3>附录 C：资料来源</h3>
    <ul>
      <li>TypeSafe AI 官方博客《Introducing System One Models &amp; Jev》（2026-09-15）与官方主页</li>
      <li>TechCrunch、The Register、Forbes、Business Standard、The Indian Express 等报道</li>
      <li>Vercel AI Gateway 采用数据、Bryo AI 对比测试、Every（Mike Taylor / Dan Shipper）独立评测</li>
      <li>Hacker News 讨论帖（1821 分 / 480 评论）及创始人 CompleteSkeptic 答疑</li>
      <li>Armin Ronacher（Earendil CTO）、Theo Browne 等从业者分析</li>
      <li>央广网 2026-09-20《Jev 模型是什么？TypeSafe「决策模型」爆火后，APUS 交出全球首批跨平台开源复现》</li>
      <li>国内社区实测与复盘文章（至顶网、CSDN、AGI Hunt 等）</li>
    </ul>
  </section>

  <footer>
    <div class="src">
      <strong>免责说明：</strong>本报告基于 Jev 发布后 7 天内的公开信息整理。其中大量性能数据来自厂商自建评测，
      尚无充分的第三方独立验证；官方自定义评测的口径偏向已在正文中逐条标注。随模型迭代与独立评测出现，
      部分结论可能需要修订。
    </div>
    <div>内容截止：2026-09-22 ｜ HTML 版生成：2026-09-23 ｜ 分析对象：TypeSafe AI Jev（2026-09-15 发布）</div>
  </footer>

  </main>
</div>

<script src="./mermaid.min.js"></script>
<script>
  document.addEventListener('DOMContentLoaded', function () {
    if (typeof mermaid === 'undefined') {
      document.querySelectorAll('.mermaid').forEach(function (el) {
        el.outerHTML = '<p style="color:#c0392b;font-size:13px;text-align:center">图表渲染库未找到（mermaid.min.js 需与本文件同目录）。</p>';
      });
      return;
    }
    mermaid.initialize({
      startOnLoad: false,
      theme: 'base',
      themeVariables: {
        background: '#ffffff',
        primaryColor: '#eef0ff',
        primaryTextColor: '#1a1d24',
        primaryBorderColor: '#5b5bd6',
        secondaryColor: '#e7f5fb',
        secondaryTextColor: '#14495c',
        secondaryBorderColor: '#0e7fa8',
        tertiaryColor: '#f5f6f9',
        tertiaryTextColor: '#4a5163',
        tertiaryBorderColor: '#d9dce7',
        lineColor: '#6b7280',
        textColor: '#1a1d24',
        mainBkg: '#eef0ff',
        nodeBorder: '#5b5bd6',
        clusterBkg: '#f5f6f9',
        clusterBorder: '#d9dce7',
        edgeLabelBackground: '#ffffff',
        fontFamily: 'system-ui,-apple-system,"Segoe UI","PingFang SC","Microsoft YaHei",sans-serif',
        fontSize: '14px'
      },
      flowchart: { htmlLabels: true, useMaxWidth: true, curve: 'basis', padding: 14, nodeSpacing: 42, rankSpacing: 46 },
      timeline: { useMaxWidth: true }
    });
    mermaid.run({ querySelector: '.mermaid' });
  });
</script>
</body>
</html>
]]></description>
</item><item>
    <title>把 HTML 页面发布为博客文章</title>
    <link>http://leoyim.cn/publish-html-as-blog-post/</link>
    <pubDate>Tue, 22 Sep 2026 14:00:00 &#43;0800</pubDate>
    <author>作者</author>
    <guid>http://leoyim.cn/publish-html-as-blog-post/</guid>
    <description><![CDATA[<h2 id="前言">前言</h2>
<p>这个博客一直只支持写 Markdown。但 AI 生成的内容越来越常是完整的 HTML —— 交互式图表、可视化报告、小工具、自带设计风格的页面，这些用 Markdown 都表达不出来。</p>
<p>于是给博客加了一种新的内容类型：<strong>直接把 HTML 文档当作一篇文章发布</strong>。</p>
<p>技术上只有一个核心约束值得先说清楚：一份完整的 HTML 文档自带 <code>&lt;!DOCTYPE&gt;</code>、<code>&lt;head&gt;</code> 和自己的全局样式（<code>*</code>、<code>body</code>、<code>h1</code> 这类选择器）。如果直接粘贴进 Markdown 正文，会同时发生两件坏事：</p>
<ul>
<li>提前闭合博客模板的 DOM 结构，整页布局错乱；</li>
<li>全局样式与主题互相覆盖，导航栏、字体、代码块样式都可能被改掉。</li>
</ul>
<p>所以关键不是&quot;怎么把 HTML 塞进去&quot;，而是<strong>怎么隔离</strong>。隔离做对了，剩下的都是细节。</p>
<h2 id="三种发布方式">三种发布方式</h2>
<table>
<thead>
<tr>
<th>方式</th>
<th>适用场景</th>
<th>进文章列表</th>
<th>评论区 / 目录</th>
<th>独立 URL</th>
</tr>
</thead>
<tbody>
<tr>
<td>① 嵌入文章（iframe shortcode）</td>
<td>文章主体是文字，HTML 是配图或配工具</td>
<td>是</td>
<td>正常</td>
<td>有，位于文章目录下</td>
</tr>
<tr>
<td>② <code>raw</code> 布局整页输出</td>
<td>HTML 本身就是那篇文章</td>
<td>是（需手写 <code>summary</code>）</td>
<td>无</td>
<td>有，就是文章 URL</td>
</tr>
<tr>
<td>③ 放进 <code>static/</code></td>
<td>纯工具页，不打算进文章系统</td>
<td>否</td>
<td>无</td>
<td>有</td>
</tr>
</tbody>
</table>
<p>三种方式可以混用，日常用得最多的是 ①。</p>
<h2 id="方式一嵌入到文章里">方式一：嵌入到文章里</h2>
<h3 id="目录结构">目录结构</h3>
<p>把文章做成 page bundle，HTML 与正文平级放：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-text" data-lang="text">content/posts/publish-html-as-blog-post/
├── index.zh-cn.md      # 文章正文
└── demo.html           # AI 生成的 HTML，原样保存，不做任何修改
</code></pre></td></tr></table>
</div>
</div><p>与 <code>index.zh-cn.md</code> 同目录的所有文件都会成为这个页面的<strong>资源（page resources）</strong>，随文章一起发布。HTML 文件不需要改后缀、不需要剥掉 <code>&lt;head&gt;</code>、不需要把 CSS 内联化。</p>
<h3 id="在正文中调用">在正文中调用</h3>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-markdown" data-lang="markdown">{{<span class="p">&lt;</span> <span class="nt">rawhtml</span> <span class="err">&#34;</span><span class="na">demo</span><span class="err">.</span><span class="na">html</span><span class="err">&#34;</span> <span class="p">&gt;</span>}}
</code></pre></td></tr></table>
</div>
</div><p>下面是本篇文章自带的一个真实示例：示例内部可以点击交互，右下角的图标按钮可以打开完整页面。</p>
<div class="rawhtml-embed">
    <iframe class="rawhtml-iframe" src="/publish-html-as-blog-post/demo.html" title="把 HTML 页面发布为博客文章" loading="lazy"
        style="width: 100%; border: 0; display: block; background: #fff; min-height: 480px;" data-rawhtml-fit="true"></iframe>
    <a class="rawhtml-embed-open" href="/publish-html-as-blog-post/demo.html" target="_blank" rel="noopener noreferrer" title="在新标签页中打开" aria-label="在新标签页中打开">
        <i class="fas fa-external-link-alt" aria-hidden="true"></i>
    </a>
</div>
<h3 id="参数说明">参数说明</h3>
<table>
<thead>
<tr>
<th>参数</th>
<th>必填</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>第 1 个位置参数 / <code>src</code></td>
<td>是</td>
<td>资源文件名；或 <code>/html/xxx.html</code> 站内路径；或完整外链 URL</td>
</tr>
<tr>
<td><code>height</code></td>
<td>否</td>
<td>固定高度（px）。传了就关闭高度自适应</td>
</tr>
<tr>
<td><code>label</code></td>
<td>否</td>
<td>右下角悬浮按钮的提示文案（tooltip 与无障碍标签），默认&quot;在新标签页中打开&quot;</td>
</tr>
<tr>
<td><code>title</code></td>
<td>否</td>
<td>iframe 的 <code>title</code>，用于无障碍，默认取文章标题</td>
</tr>
</tbody>
</table>
<p>行为细节：</p>
<ul>
<li>参数是<strong>文件名</strong>时只在当前文章的资源里查找，找不到会让 Hugo 构建直接失败，避免出现死链；</li>
<li>默认自适应高度：同源 iframe 可以读取内部 <code>scrollHeight</code>，并用 <code>ResizeObserver</code> 跟随内容变化；底部兜底 <code>min-height: 480px</code>；</li>
<li>如果 HTML 是 <code>html, body { height: 100% }</code> 的整屏应用，内部高度恒等于视口高度，自适应没有意义，请显式传 <code>height=&quot;800&quot;</code> 这类固定值；</li>
<li>嵌入外链（跨域）时读不到内部高度，会保留 <code>min-height</code>。</li>
</ul>
<h3 id="独立页面展示">独立页面展示</h3>
<p>每个页面资源都有自己的 URL，iframe 右下角会自动浮现一个&quot;在新标签页打开&quot;的图标按钮，点开就是原样的 HTML 页面：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-text" data-lang="text">https://leoyim.cn/publish-html-as-blog-post/demo.html
</code></pre></td></tr></table>
</div>
</div><p>相对路径引用的图片、字体等资源都能正常工作，因为它们和 HTML 在同一目录下一起发布。</p>
<h2 id="方式二raw-布局让-html-就是文章本身">方式二：raw 布局，让 HTML 就是文章本身</h2>
<p>方式一的文章 URL 打开时是套着主题外壳的（导航栏、标题、目录、评论）。如果希望这篇文章的正文<strong>完全由 HTML 决定</strong>，比如整屏的交互体验，就用 <code>layout: &quot;raw&quot;</code>。</p>
<p>新建 <code>content/posts/my-report/index.html</code>（注意后缀是 <code>.html</code>，不是 <code>.md</code>）：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt"> 1
</span><span class="lnt"> 2
</span><span class="lnt"> 3
</span><span class="lnt"> 4
</span><span class="lnt"> 5
</span><span class="lnt"> 6
</span><span class="lnt"> 7
</span><span class="lnt"> 8
</span><span class="lnt"> 9
</span><span class="lnt">10
</span><span class="lnt">11
</span><span class="lnt">12
</span><span class="lnt">13
</span><span class="lnt">14
</span><span class="lnt">15
</span><span class="lnt">16
</span><span class="lnt">17
</span><span class="lnt">18
</span><span class="lnt">19
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-html" data-lang="html">---
title: &#34;一份交互式报告&#34;
date: 2026-09-22T14:00:00+08:00
layout: &#34;raw&#34;
summary: &#34;这里是列表页显示的摘要，因为正文是 HTML，Hugo 自动生成的摘要通常不可读，建议手写。&#34;
hiddenFromSearch: true
---

<span class="cp">&lt;!DOCTYPE html&gt;</span>
<span class="p">&lt;</span><span class="nt">html</span> <span class="na">lang</span><span class="o">=</span><span class="s">&#34;zh-CN&#34;</span><span class="p">&gt;</span>
<span class="p">&lt;</span><span class="nt">head</span><span class="p">&gt;</span>
    <span class="p">&lt;</span><span class="nt">meta</span> <span class="na">charset</span><span class="o">=</span><span class="s">&#34;utf-8&#34;</span><span class="p">&gt;</span>
    <span class="p">&lt;</span><span class="nt">title</span><span class="p">&gt;</span>一份交互式报告<span class="p">&lt;/</span><span class="nt">title</span><span class="p">&gt;</span>
    <span class="p">&lt;</span><span class="nt">style</span><span class="p">&gt;</span><span class="c">/* 你/ AI 自己的全局样式 */</span><span class="p">&lt;/</span><span class="nt">style</span><span class="p">&gt;</span>
<span class="p">&lt;/</span><span class="nt">head</span><span class="p">&gt;</span>
<span class="p">&lt;</span><span class="nt">body</span><span class="p">&gt;</span>
    <span class="c">&lt;!-- 整个页面的内容 --&gt;</span>
<span class="p">&lt;/</span><span class="nt">body</span><span class="p">&gt;</span>
<span class="p">&lt;/</span><span class="nt">html</span><span class="p">&gt;</span>
</code></pre></td></tr></table>
</div>
</div><p>要点：</p>
<ul>
<li><code>index.html</code> 里 front matter 之后的内容，Hugo 不做 Markdown 渲染，原样输出；</li>
<li><code>layout: &quot;raw&quot;</code> 对应新增的 <code>layouts/posts/raw.html</code> —— 一个只有一行 <code>{{ .Content }}</code> 的极简布局，不套导航栏、页脚、目录、评论；</li>
<li>文章 URL 就是这份 HTML，所以是干净的独立地址，而不是 <code>xxx.html</code> 这样的资源地址；</li>
<li>由于正文是 HTML，列表页摘要和搜索索引都会受影响，建议手写 <code>summary</code> 并设置 <code>hiddenFromSearch: true</code>。</li>
</ul>
<h2 id="方式三放进-static-的独立页面">方式三：放进 <code>static/</code> 的独立页面</h2>
<p>如果这个 HTML 只是工具页，不希望它出现在文章列表、RSS、搜索里：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-text" data-lang="text">static/html/tool.html  →  https://leoyim.cn/html/tool.html
</code></pre></td></tr></table>
</div>
</div><p><code>static/</code> 下的文件会被原样复制到站点根目录，零配置。适合&quot;临时给别人看一个页面&quot;的场景。</p>
<h2 id="注意事项">注意事项</h2>
<ul>
<li><strong>样式隔离是单向的</strong>：iframe 里的样式出不来，主题样式也进不去。iframe 内部不继承博客的字体和深色模式，HTML 必须自带完整样式；</li>
<li><strong>深色模式</strong>：iframe 默认设置了白色底（<code>background: #fff</code>），避免透明底的 HTML 在主题深色背景下文字看不清；</li>
<li><strong>搜索</strong>：iframe 里的内容不会被 Algolia 索引（索引来自渲染后的文字），也不会进目录 TOC。建议在正文里补一段文字摘要；</li>
<li><strong>RSS</strong>：<code>rssFullText = true</code> 时，阅读器里无法渲染 iframe，订阅者只会看到正文文字，必要时注明&quot;访问原文查看交互内容&quot;；</li>
<li><strong>评论与分享</strong>：方式一下完全正常，指向的是文章 URL 本身；</li>
<li><strong>安全</strong>：iframe 没有加 <code>sandbox</code>，因为需要执行脚本。只嵌入自己生成或可信来源的 HTML，不要嵌入来路不明的文档；</li>
<li><strong>字数统计</strong>：iframe 内容不计入 <code>WordCount</code>，正文太短时文章列表上的&quot;阅读时间&quot;会失真。</li>
</ul>
<h2 id="小结">小结</h2>
<p>整个改造只涉及三个文件：</p>
<table>
<thead>
<tr>
<th>文件</th>
<th>作用</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>layouts/shortcodes/rawhtml.html</code></td>
<td>iframe 嵌入一篇 HTML，含高度自适应与独立页面链接</td>
</tr>
<tr>
<td><code>layouts/posts/raw.html</code></td>
<td><code>layout: &quot;raw&quot;</code> 时整页原样输出</td>
</tr>
<tr>
<td><code>content/posts/&lt;slug&gt;/xxx.html</code></td>
<td>HTML 与 <code>index.&lt;lang&gt;.md</code> 平级放置即成为页面资源</td>
</tr>
</tbody>
</table>
<p>日常使用只有两步：把 AI 生成的 HTML 存成与 <code>index.md</code> 同目录的文件，然后在正文里写一行 <code>{{&lt; rawhtml &quot;xxx.html&quot; &gt;}}</code>。样式、脚本、<code>&lt;!DOCTYPE&gt;</code> 全都不用动。</p>
]]></description>
</item><item>
    <title>Tekton 本地部署与实践笔记</title>
    <link>http://leoyim.cn/tekton-local-deploy-practice/</link>
    <pubDate>Fri, 07 Aug 2026 19:00:00 &#43;0800</pubDate>
    <author>作者</author>
    <guid>http://leoyim.cn/tekton-local-deploy-practice/</guid>
    <description><![CDATA[<blockquote>
<p>环境：Windows + Docker Desktop 内置 Kubernetes（kind 内核）
目标：在本地 K8s 上部署 Tekton，验证端到端工具链，并跑通一条能体现 Tekton 价值的示例流水线。
完成状态：全部验证通过 ✅</p>
</blockquote>
<h2 id="0-背景与目标">0. 背景与目标</h2>
<p>本次实践要解决的是「在本地 Kubernetes 环境中把 Tekton 跑起来并验证可用」。具体验收项：</p>
<ol>
<li>安装 Tekton Pipelines 核心组件到现有集群</li>
<li>确认所有 Tekton Pod 处于 Running</li>
<li>验证 Tekton CLI（tkn）可连接集群并执行命令</li>
<li>确认可通过 kubectl 访问 Tekton 的 CRD（Task / Pipeline 等）</li>
<li>运行一个简单的 Tekton Task 验证工具链</li>
<li>（扩展）安装 Tekton Dashboard 图形界面</li>
<li>（扩展）创建一条示例流水线 <code>demo-pipeline</code> 体现 Tekton 价值</li>
</ol>
<!--more-->
<h2 id="1-环境判定第一个关键认知">1. 环境判定（第一个关键认知）</h2>
<p>用户提到「已通过 kind 部署启动了 Kubernetes 集群」，但实际检测到的唯一 Kubernetes 上下文是 <strong><code>docker-desktop</code></strong>（节点名为 <code>desktop-control-plane</code>），并没有独立创建的 kind 集群，也没有 <code>kind</code> CLI。</p>
<p>关键证据：集群里跑着 <code>kindnet</code> 这个 CNI Pod，说明 <strong>Docker Desktop 内置的 Kubernetes 就是用 kind 内核构建的</strong>。</p>
<p>结论：Tekton 的部署命令、验证步骤与真正的 kind 集群完全一致，无需另外安装 <code>kind</code> CLI 或新建集群。直接在这个 <code>docker-desktop</code> 集群上完成全部工作即可。</p>
<h2 id="2-部署-tekton-pipelines-v160">2. 部署 Tekton Pipelines v1.6.0</h2>
<p>通过官方发布清单一键部署：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">kubectl apply -f https://storage.googleapis.com/tekton-releases/pipeline/latest/release.yaml
</code></pre></td></tr></table>
</div>
</div><ul>
<li>部署到命名空间 <code>tekton-pipelines</code> 与 <code>tekton-pipelines-resolvers</code></li>
<li>核心镜像来自 <code>ghcr.io/tektoncd/pipeline/*</code>（在本机网络下可正常拉取）</li>
<li>包含 8 个 CRD，以及 <code>tekton-pipelines-controller</code>、<code>tekton-pipelines-webhook</code>、<code>tekton-pipelines-resolvers</code> 等核心组件</li>
</ul>
<p></p>
<blockquote>
<p>图 1：本地 Tekton 部署架构总览（组件与命名空间示意；Dashboard 实际端口为 <code>9097</code>）。</p>
</blockquote>
<h2 id="3-安装-tekton-clitknv0450">3. 安装 Tekton CLI（tkn）v0.45.0</h2>
<p>从 GitHub Releases 下载 Windows 版压缩包，解压 <code>tkn.exe</code> 到 <code>C:\Users\leoya\bin</code>（该目录已在 PATH 中）。</p>
<p>踩到的网络坑：GitHub API（<code>api.github.com</code>）被限流返回 403，无法直接拿到最新版本号。改用「探测候选版本号 + 直接 GET 下载」的方式，慢网下用 <code>curl -C -</code> 断点续传，最终成功拉取并校验 <code>v0.45.0</code>。</p>
<p>验证：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">tkn version
<span class="c1"># Client version: 0.45.0</span>
<span class="c1"># Pipeline version: v1.6.0</span>
</code></pre></td></tr></table>
</div>
</div><h2 id="4-五项验收验证全部通过-">4. 五项验收验证（全部通过 ✅）</h2>
<table>
<thead>
<tr>
<th>验收项</th>
<th>结果</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>Tekton Pod 状态</td>
<td>✅</td>
<td>4/4 Running &amp; Ready（controller / webhook / events-controller / remote-resolvers）</td>
</tr>
<tr>
<td>tkn 连接集群</td>
<td>✅</td>
<td><code>tkn version</code> 显示 Client 0.45.0 / Pipeline v1.6.0，可正常查询</td>
</tr>
<tr>
<td>CRD 可访问</td>
<td>✅</td>
<td>8 个 <code>tekton.dev</code> CRD（tasks / taskruns / pipelines / pipelineruns / customruns / stepactions / verificationpolicies / resolutionrequests）均注册可读</td>
</tr>
<tr>
<td>示例 Task</td>
<td>✅</td>
<td><code>hello-task</code> 的 TaskRun <strong>Succeeded</strong>，输出正确</td>
</tr>
<tr>
<td>Dashboard（扩展）</td>
<td>✅</td>
<td>v0.71.0 已装，http://localhost:9097 可访问</td>
</tr>
</tbody>
</table>
<h2 id="5-踩坑与修复cgrdev-镜像拉取失败">5. 踩坑与修复：cgr.dev 镜像拉取失败</h2>
<p>这是本次最值得记录的一个问题。</p>
<p><strong>现象</strong>：首次运行 Task 时，Pod 卡在 <code>ContainerCreating</code> / <code>ImagePullBackOff</code>，报错 <code>unexpected EOF</code>。定位到是 Tekton 注入的 <code>place-scripts</code> 初始化容器在拉取 <code>cgr.dev/chainguard/busybox</code> 时，本机网络对该 registry 不稳定导致传输中断。</p>
<p><strong>根因</strong>：控制器 Deployment 的 <code>-shell-image</code> 参数（args 索引 9）默认硬编码为 <code>cgr.dev/chainguard/busybox</code>，这个镜像被所有 Task 的 <code>place-scripts</code> 初始化容器依赖，也就是说<strong>哪怕你用 <code>command</code>/<code>args</code> 写 Task，也绕不开它</strong>。</p>
<p><strong>修复</strong>：把 <code>-shell-image</code> 改为网络更稳定的 <code>docker.io/library/busybox:1.36.1</code>，再滚动重启控制器：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span><span class="lnt">5
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">kubectl patch deployment tekton-pipelines-controller -n tekton-pipelines <span class="se">\
</span><span class="se"></span>  --type<span class="o">=</span>json <span class="se">\
</span><span class="se"></span>  -p<span class="o">=</span><span class="s1">&#39;[{&#34;op&#34;:&#34;replace&#34;,&#34;path&#34;:&#34;/spec/template/spec/containers/0/args/9&#34;,&#34;value&#34;:&#34;docker.io/library/busybox:1.36.1&#34;}]&#39;</span>

kubectl rollout restart deployment/tekton-pipelines-controller -n tekton-pipelines
</code></pre></td></tr></table>
</div>
</div><p>重启后 Task 立即恢复正常。</p>
<p><strong>教训</strong>：遇到 <code>place-scripts</code> 拉取 busybox 失败，不要去改 Task 写法，要从控制器 <code>-shell-image</code> 入手。</p>
<h2 id="6-安装-tekton-dashboard图形界面">6. 安装 Tekton Dashboard（图形界面）</h2>
<p>Tekton 默认没有图形界面，需要单独安装 Dashboard。</p>
<p>注意：官方文档里的 <code>storage.googleapis.com/tekton-releases/dashboard/latest/...</code> 路径目前已返回 404，需改用 GitHub Release 资产：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">kubectl apply -f https://github.com/tektoncd/dashboard/releases/download/v0.71.0/release.yaml
</code></pre></td></tr></table>
</div>
</div><ul>
<li>部署到 <code>tekton-pipelines</code>，Service 名为 <code>tekton-dashboard</code>，端口 <code>9097</code></li>
<li>镜像来自 <code>ghcr.io/tektoncd/dashboard/*</code>，无 cgr.dev 依赖，可正常拉取</li>
<li>启动端口转发（后台运行）：</li>
</ul>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">kubectl port-forward -n tekton-pipelines svc/tekton-dashboard 9097:9097
</code></pre></td></tr></table>
</div>
</div><p>浏览器访问 <strong>http://localhost:9097</strong> 即可看到 Pipeline / Task / TaskRun 等资源的图形界面。若转发随终端会话断开，重跑上面那条命令即可恢复。</p>
<h2 id="7-示例流水线-demo-pipeline体现价值">7. 示例流水线 demo-pipeline（体现价值）</h2>
<p>为了真正体现 Tekton 的价值，而不是只跑一个 <code>echo</code>，我创建了一条由三个 Task 组成的流水线：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback">git-clone  →  analyze-repo  →  report-summary
 (Catalog)      (自定义)          (自定义)
</code></pre></td></tr></table>
</div>
</div><ul>
<li><strong>clone</strong>：直接复用 Tekton Catalog 官方 <code>git-clone</code> Task，把仓库克隆进 Workspace</li>
<li><strong>analyze</strong>：自定义 Task，统计仓库文件数并找出最大文件，把结果写入 <code>results</code></li>
<li><strong>report</strong>：自定义 Task，消费上游 <code>results</code> 打印汇总报告</li>
</ul>
<p></p>
<blockquote>
<p>图 2：demo-pipeline 流程图，演示 Task 复用、Workspace 共享、runAfter 编排与 Results 传递。</p>
</blockquote>
<p>这条流水线集中体现了 Tekton 的六大价值点：</p>
<ol>
<li><strong>可复用 Task</strong>：官方 <code>git-clone</code> 直接从 Catalog 引入，不重复造轮子</li>
<li><strong>Workspace</strong>：用 <code>volumeClaimTemplate</code> 自动创建/回收 1Gi PVC，跨三个 Task 共享同一份代码</li>
<li><strong>Pipeline DAG</strong>：用 <code>runAfter</code> 显式声明依赖（克隆完成才开始分析）</li>
<li><strong>Parameters</strong>：仓库地址、业务消息参数化，一套流水线跑任意仓库</li>
<li><strong>Results 传递</strong>：<code>analyze</code> 算出的「文件数 / 最大文件」通过 <code>$(tasks.analyze.results.*)</code> 解耦传给 <code>report</code></li>
<li><strong>K8s 原生 CRD</strong>：全部是 Pipeline/Task/TaskRun/PipelineRun 资源，没有 Jenkins 那样的中心化 CI Master</li>
</ol>
<p>两个 PipelineRun 均 <code>Succeeded</code>，报告正确输出：<code>仓库 https://github.com/octocat/Hello-World.git</code> · <code>文件总数 24</code> · <code>最大文件 ./.git/hooks/pre-rebase.sample</code> · 状态成功 ✅。</p>
<p><strong>这一节又踩了两个小坑</strong>：</p>
<ul>
<li>多文档 YAML 必须用 <code>---</code> 分隔，否则 <code>kubectl apply</code> 只会解析最后一个文档（最初漏写 <code>---</code>，导致两个自定义 Task 没被创建，只建了 Pipeline）</li>
<li>Alpine/busybox 的 <code>find</code> 不支持 <code>-printf</code>，统计最大文件改用 <code>find -exec stat -c '%s %n' {} + | sort -rn</code></li>
</ul>
<h2 id="8-命令速查">8. 命令速查</h2>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt"> 1
</span><span class="lnt"> 2
</span><span class="lnt"> 3
</span><span class="lnt"> 4
</span><span class="lnt"> 5
</span><span class="lnt"> 6
</span><span class="lnt"> 7
</span><span class="lnt"> 8
</span><span class="lnt"> 9
</span><span class="lnt">10
</span><span class="lnt">11
</span><span class="lnt">12
</span><span class="lnt">13
</span><span class="lnt">14
</span><span class="lnt">15
</span><span class="lnt">16
</span><span class="lnt">17
</span><span class="lnt">18
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="c1"># 部署核心组件</span>
kubectl apply -f https://storage.googleapis.com/tekton-releases/pipeline/latest/release.yaml

<span class="c1"># 查看 Tekton Pod</span>
kubectl get pods -n tekton-pipelines -o wide

<span class="c1"># tkn 查询</span>
tkn task list
tkn pipelinerun list
tkn pipelinerun logs &lt;name&gt;

<span class="c1"># 修复 cgr.dev 镜像问题</span>
kubectl patch deployment tekton-pipelines-controller -n tekton-pipelines --type<span class="o">=</span>json <span class="se">\
</span><span class="se"></span>  -p<span class="o">=</span><span class="s1">&#39;[{&#34;op&#34;:&#34;replace&#34;,&#34;path&#34;:&#34;/spec/template/spec/containers/0/args/9&#34;,&#34;value&#34;:&#34;docker.io/library/busybox:1.36.1&#34;}]&#39;</span>
kubectl rollout restart deployment/tekton-pipelines-controller -n tekton-pipelines

<span class="c1"># Dashboard 端口转发</span>
kubectl port-forward -n tekton-pipelines svc/tekton-dashboard 9097:9097
</code></pre></td></tr></table>
</div>
</div><h2 id="9-后续可扩展方向">9. 后续可扩展方向</h2>
<ul>
<li><strong>并行执行</strong>：在一条 Pipeline 里同时跑 lint / test / build（体现 Tekton 的并行 DAG 能力）</li>
<li><strong>条件分支</strong>：用 <code>when</code> 表达式，仅在 <code>main</code> 分支执行部署 Task</li>
<li><strong>构建镜像</strong>：接入 Kaniko Task，把代码构建成镜像并推送到镜像仓库</li>
<li><strong>事件触发</strong>：安装 Tekton Triggers，用 Git Webhook 自动触发流水线</li>
</ul>
<h2 id="10-本地恢复与运维2026-08-20-补充">10. 本地恢复与运维（2026-08-20 补充）</h2>
<blockquote>
<p>本章记录重启 Docker Desktop 后的排障、一键恢复脚本、WSL2 迁移，以及 Dashboard 只读问题修复。</p>
</blockquote>
<h3 id="101-重启-docker-desktop-后-tekton消失的真相">10.1 重启 Docker Desktop 后 Tekton「消失」的真相</h3>
<p><strong>现象</strong>：重启 Docker Desktop 后，用 <code>docker ps -a</code> 看不到任何「tekton」实例，以为 Tekton 丢了。</p>
<p><strong>真相</strong>：Tekton 从来不是 Docker 容器，它是跑在 Docker Desktop 内置 Kubernetes（<code>docker-desktop</code> 集群）里的 K8s 工作负载（Deployment / Pod / CRD）。<code>docker ps -a</code> 只列 Docker Engine 直接管理的容器，Tekton 组件不会以「tekton」名字出现，所以「看不到」是<strong>查看工具用错了</strong>，不是真的消失。</p>
<p>实测诊断（WSL2 / Git Bash 直连本机集群）：</p>
<table>
<thead>
<tr>
<th>检查项</th>
<th>结果</th>
</tr>
</thead>
<tbody>
<tr>
<td>集群 <code>docker-desktop</code></td>
<td>Ready（节点 <code>desktop-control-plane</code>）</td>
</tr>
<tr>
<td><code>tekton-pipelines</code> 命名空间</td>
<td>Active</td>
</tr>
<tr>
<td>4 个 Pod（controller/webhook/events/dashboard）</td>
<td>全 <code>1/1 Running</code>（随 Docker 重启一起重启过）</td>
</tr>
<tr>
<td><code>-shell-image</code> 补丁</td>
<td>仍持久化为 <code>docker.io/library/busybox:1.36.1</code></td>
</tr>
<tr>
<td>tkn / Pipeline / Dashboard</td>
<td>0.45.0 / v1.6.0 / v0.71.0</td>
</tr>
<tr>
<td>8 个 tekton.dev CRD</td>
<td>齐全</td>
</tr>
</tbody>
</table>
<blockquote>
<p>结论：本地 docker-desktop K8s 正常重启后会自动恢复状态（Pod 重启但会回来）。如果哪天真「全没了」（命名空间都没了），那是集群被 <strong>Reset / 自动更新重建 VM / K8s 开关被关再开</strong>，属于硬重置，需要重新部署。</p>
</blockquote>
<p><strong>正确的查看命令</strong>（别再用 <code>docker ps -a</code>）：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">kubectl get pods -n tekton-pipelines -o wide
kubectl get ns
kubectl config current-context      <span class="c1"># 应为 docker-desktop</span>
kubectl get nodes
</code></pre></td></tr></table>
</div>
</div><h3 id="102-一键恢复脚本--wsl2-迁移">10.2 一键恢复脚本 + WSL2 迁移</h3>
<p>裸 <code>kubectl apply</code> 远程 URL 的方案，一旦集群被硬重置就一切归零。因此把恢复流程固化为脚本，并迁入 WSL2 Ubuntu（跑 kubectl 比走 <code>/mnt/d</code> 更快）：</p>
<p>脚本位置（WSL2）：<code>/home/leoyim/tekton/</code></p>
<ul>
<li><code>tekton-install.sh</code>：幂等恢复，切 <code>docker-desktop</code> 上下文 → 下载并 apply Pipeline + Dashboard 清单（缓存到 <code>manifests/</code>）→ 自动 patch <code>-shell-image</code> → rollout restart → 等待就绪</li>
<li><code>tekton-forward.sh</code>：Dashboard 端口转发到 <code>localhost:9097</code></li>
<li><code>demo-pipeline.yaml</code>：按第 7 节重建的示例流水线</li>
</ul>
<p>Windows 源副本仍在 <code>D:\Project\NoteBook\05Tekton\</code>（备份 + 与笔记同目录）。</p>
<p>WSL2 内 kubectl 位于 <code>/usr/local/bin/kubectl</code>，上下文即 <code>docker-desktop</code>，脚本可直接运行。</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="c1"># WSL2 终端</span>
<span class="nb">cd</span> ~/tekton
bash tekton-install.sh     <span class="c1"># 集群被硬重置时一键还原（平时可跳过，纯验证用）</span>
bash tekton-forward.sh     <span class="c1"># 另开终端：转发 Dashboard，浏览器开 http://localhost:9097</span>
</code></pre></td></tr></table>
</div>
</div><blockquote>
<p>注意：Dashboard 默认是<strong>只读部署</strong>（见 10.3），<code>tekton-install.sh</code> 目前只还原「能看」，如需界面可写，恢复后再跑 10.3 的两步。</p>
</blockquote>
<h3 id="103-dashboard-只读模式与修复ui-可写">10.3 Dashboard 只读模式与修复（UI 可写）</h3>
<p><strong>现象</strong>：Dashboard 界面顶部提示 Read Only，不能从网页创建/编辑/删除资源。</p>
<p><strong>根因</strong>：Tekton Dashboard 官方 release 默认就是只读部署，由「标志位 + RBAC」双重控制：</p>
<ul>
<li>部署参数含 <code>--read-only=true</code>（args 索引 8）</li>
<li><code>tekton-dashboard</code> ServiceAccount 只绑定 <code>*-view</code>（只读）ClusterRole，无任何 edit 绑定</li>
</ul>
<blockquote>
<p>只读<strong>只影响网页 UI 写操作</strong>；终端 <code>kubectl</code>/<code>tkn</code>（docker-desktop kubeconfig 是集群管理员）始终是全读写，用 <code>kubectl apply</code> 跑 <code>demo-pipeline.yaml</code> 不受影响。</p>
</blockquote>
<p><strong>改为可写（两步缺一不可）</strong>：</p>
<p>① 给 <code>tekton-dashboard</code> SA 加 edit 权限：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt"> 1
</span><span class="lnt"> 2
</span><span class="lnt"> 3
</span><span class="lnt"> 4
</span><span class="lnt"> 5
</span><span class="lnt"> 6
</span><span class="lnt"> 7
</span><span class="lnt"> 8
</span><span class="lnt"> 9
</span><span class="lnt">10
</span><span class="lnt">11
</span><span class="lnt">12
</span><span class="lnt">13
</span><span class="lnt">14
</span><span class="lnt">15
</span><span class="lnt">16
</span><span class="lnt">17
</span><span class="lnt">18
</span><span class="lnt">19
</span><span class="lnt">20
</span><span class="lnt">21
</span><span class="lnt">22
</span><span class="lnt">23
</span><span class="lnt">24
</span><span class="lnt">25
</span><span class="lnt">26
</span><span class="lnt">27
</span><span class="lnt">28
</span><span class="lnt">29
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">kubectl apply -f - <span class="s">&lt;&lt;&#39;EOF&#39;
</span><span class="s">apiVersion: rbac.authorization.k8s.io/v1
</span><span class="s">kind: ClusterRole
</span><span class="s">metadata:
</span><span class="s">  name: tekton-dashboard-edit
</span><span class="s">rules:
</span><span class="s">  - apiGroups: [&#34;tekton.dev&#34;]
</span><span class="s">    resources: [&#34;*&#34;]
</span><span class="s">    verbs: [&#34;get&#34;,&#34;list&#34;,&#34;watch&#34;,&#34;create&#34;,&#34;update&#34;,&#34;patch&#34;,&#34;delete&#34;]
</span><span class="s">  - apiGroups: [&#34;&#34;]
</span><span class="s">    resources: [&#34;pods&#34;,&#34;pods/log&#34;,&#34;configmaps&#34;,&#34;secrets&#34;,&#34;persistentvolumeclaims&#34;,&#34;services&#34;]
</span><span class="s">    verbs: [&#34;get&#34;,&#34;list&#34;,&#34;watch&#34;,&#34;create&#34;,&#34;update&#34;,&#34;patch&#34;,&#34;delete&#34;]
</span><span class="s">  - apiGroups: [&#34;batch&#34;]
</span><span class="s">    resources: [&#34;jobs&#34;]
</span><span class="s">    verbs: [&#34;*&#34;]
</span><span class="s">---
</span><span class="s">apiVersion: rbac.authorization.k8s.io/v1
</span><span class="s">kind: ClusterRoleBinding
</span><span class="s">metadata:
</span><span class="s">  name: tekton-dashboard-edit-binding
</span><span class="s">roleRef:
</span><span class="s">  apiGroup: rbac.authorization.k8s.io
</span><span class="s">  kind: ClusterRole
</span><span class="s">  name: tekton-dashboard-edit
</span><span class="s">subjects:
</span><span class="s">  - kind: ServiceAccount
</span><span class="s">    name: tekton-dashboard
</span><span class="s">    namespace: tekton-pipelines
</span><span class="s">EOF</span>
</code></pre></td></tr></table>
</div>
</div><p>② 把 <code>--read-only=true</code> 改为 <code>false</code> 并重启：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">kubectl patch deployment tekton-dashboard -n tekton-pipelines --type<span class="o">=</span>json <span class="se">\
</span><span class="se"></span>  -p<span class="o">=</span><span class="s1">&#39;[{&#34;op&#34;:&#34;replace&#34;,&#34;path&#34;:&#34;/spec/template/spec/containers/0/args/8&#34;,&#34;value&#34;:&#34;--read-only=false&#34;}]&#39;</span>
kubectl rollout restart deployment/tekton-dashboard -n tekton-pipelines
</code></pre></td></tr></table>
</div>
</div><p>刷新 http://localhost:9097，Read Only 提示消失即生效。该改动存于 etcd，Pod 重启不丢；但<strong>集群被硬重置后会回来</strong>，届时需要重跑这两步（或把它并进 <code>tekton-install.sh</code>）。</p>
<h3 id="104-demo-pipelineyaml-重建">10.4 demo-pipeline.yaml 重建</h3>
<p>原笔记第 7 节只有流程描述、没存完整清单。已按第 7 节重建 <code>demo-pipeline.yaml</code>（<code>git-clone → analyze-repo → report-summary</code>），放 <code>~/tekton/</code> 与 <code>D:\Project\NoteBook\05Tekton\</code>。</p>
<p>首次跑前需先装官方 <code>git-clone</code> Catalog Task：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">kubectl apply -f https://raw.githubusercontent.com/tektoncd/catalog/main/task/git-clone/0.9/git-clone.yaml
kubectl apply -f demo-pipeline.yaml
</code></pre></td></tr></table>
</div>
</div><h2 id="附录关键经验教训清单">附录：关键经验教训清单</h2>
<ul>
<li>Docker Desktop 内置 K8s = kind 内核，Tekton 部署方式与真 kind 一致</li>
<li><code>cgr.dev/chainguard/busybox</code> 在本机网络不稳定 → 改 <code>docker.io/library/busybox:1.36.1</code></li>
<li><code>place-scripts</code> 注入无法用 <code>command</code>/<code>args</code> 绕过，必须从 <code>-shell-image</code> 解决</li>
<li>Dashboard 的 <code>latest</code> 下载路径已 404，改用 GitHub Release 资产</li>
<li>多文档 YAML 必须加 <code>---</code></li>
<li>busybox <code>find</code> 不支持 <code>-printf</code>，用 <code>stat</code> 替代</li>
<li><strong>Tekton 是 K8s 工作负载，不归 <code>docker ps</code> 管</strong>，用 <code>docker ps -a</code> 永远看不到「tekton」，正确查看用 <code>kubectl get pods -n tekton-pipelines</code></li>
<li>重启 Docker Desktop 后 Tekton 通常自动恢复；真「全没了」= 集群硬重置（Reset / 自动更新重建 VM / K8s 重开），用 <code>tekton-install.sh</code> 一键还原</li>
<li>恢复/运维脚本已迁入 WSL2 <code>/home/leoyim/tekton/</code>（<code>tekton-install.sh</code> / <code>tekton-forward.sh</code> / <code>demo-pipeline.yaml</code>），Windows 源副本在 <code>D:\Project\NoteBook\05Tekton\</code></li>
<li>Dashboard 官方默认<strong>只读</strong>（<code>--read-only=true</code> 且 SA 仅绑 <code>*-view</code>），UI 想可写需加 edit ClusterRole/Binding + 把 flag 改 false（见 10.3）</li>
<li>只读只限网页 UI；终端 <code>kubectl</code>/<code>tkn</code>（docker-desktop kubeconfig=集群管理员）始终是全读写</li>
</ul>]]></description>
</item><item>
    <title>Jenkins JVM 监控仪表板设计与实践</title>
    <link>http://leoyim.cn/jenkins-jvm-monitoring-dashboard/</link>
    <pubDate>Mon, 03 Aug 2026 15:10:00 &#43;0800</pubDate>
    <author>作者</author>
    <guid>http://leoyim.cn/jenkins-jvm-monitoring-dashboard/</guid>
    <description><![CDATA[<blockquote>
<p>目标：为 Jenkins（CI/CD 控制器）建立一套可观测的 JVM 运行时监控，覆盖内存、GC、线程、类加载、运行时资源五大维度，既能一眼看总体健康，又能下钻定位异常。
本笔记记录「怎么从零设计这套监控」，而非某次操作流水账，可直接照此搭建或迁移到其他 JVM 服务。</p>
</blockquote>
<h2 id="一整体链路">一、整体链路</h2>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span><span class="lnt">5
</span><span class="lnt">6
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback">Jenkins (插件暴露 /prometheus)
       │ jvm_* + vm_* 指标
       ▼
Prometheus  (按 metrics_path 抓取，多实例靠 label 区分)
       ▼
Grafana  (数据源 = Prometheus；模板变量 $instance 切换实例)
</code></pre></td></tr></table>
</div>
</div><ul>
<li><strong>指标来源</strong>：Jenkins 安装 Prometheus metrics 插件后，在 <code>/prometheus</code> 端点暴露 <code>jvm_*</code>（JVM JMX 指标，内存/GC/线程/类加载）与 <code>vm_*</code>（Dropwizard VM 指标，CPU/FD/uptime/heap 比率）两族指标。两者都带 <code>instance</code> 标签，可合并使用。</li>
<li><strong>数据源</strong>：Grafana 里建一个名为 <code>Prometheus</code> 的数据源（按名称引用，不要按 uid，避免迁移时 uid 失配）。Docker 部署 Grafana 时 URL 用 <code>host.docker.internal:9090</code>，宿主机直连用 <code>localhost:9090</code>。</li>
<li><strong>多实例切换</strong>：仪表板顶部放模板变量 <code>$instance = label_values(jvm_info, instance)</code>，开启 <code>multi + includeAll</code>、<code>allValue=&quot;.*&quot;</code>，所有面板查询带 <code>instance=&quot;$instance&quot;</code> 过滤。新增 Jenkins target 后下拉自动列出。</li>
</ul>
<!--more-->
<h2 id="二如何提炼指标">二、如何提炼指标</h2>
<p>抓取端点一个服务就能吐出上百个指标名，实测本端点暴露 <strong>52 个 <code>jvm_*</code> + 349 个 <code>vm_*</code> = 401 个指标名</strong>，不能全堆上去。需要提炼。</p>
<p><strong>为什么 401 个里只选 24 个？</strong></p>
<ul>
<li><code>vm_*</code> 大量是<strong>预聚合窗口变体</strong>（<code>_history</code> / <code>_window_5m</code> / <code>_window_15m</code> / <code>_window_1h</code> / <code>_x100</code>），与 Grafana 自己用 <code>rate()</code> 算的速率重复，选基础量即可。</li>
<li><code>vm_memory_pools_*</code> 是<strong>按池拆分</strong>的变体（G1 Eden/Old/Survivor/Metaspace/CodeHeap…），已被 <code>jvm_memory_pool_bytes_*</code> 覆盖。</li>
<li><code>jvm_*</code> 存在<strong>重复命名</strong>（如 <code>jvm_threads_current</code> 与 <code>jvm_threads_live_threads</code>、<code>jvm_memory_bytes_used</code> 与 <code>jvm_memory_used_bytes</code>），选其一。</li>
<li>去重后<strong>精选 24 个核心指标</strong>，覆盖五大维度，每个都配评价标准。</li>
</ul>
<p><strong>提炼四步法：</strong></p>
<ol>
<li><strong>定维度</strong>，按 JVM 子系统划「要看什么」：内存 / GC / 线程 / 类加载 / 运行时资源。</li>
<li><strong>选类型</strong>，看当前状态用 gauge；看趋势用 <code>rate(counter)[5m]</code>；看健康度优先比率（0~1）而非裸字节。</li>
<li><strong>去重聚合</strong>，同类信号选代表（线程状态用一个 bargauge 汇总，不为每状态画一条线）；相关绝对量合并到一张图加系列名（堆 Used/Committed/Max 三条线对比上限）。</li>
<li><strong>配评价标准</strong>，能量化的给阈值（比率类 0.8/0.9 三段）；不能量化的给定性判据（死锁 &gt;0 即严重；BLOCKED 高 = 锁竞争）。</li>
</ol>
<blockquote>
<p>时间窗口统一用 <code>[5m]</code>，不要裸用 <code>$__rate_interval</code>（部分版本下 Prometheus 会 400）。</p>
</blockquote>
<h2 id="三指标清单--解释--评价标准24-个">三、指标清单 · 解释 · 评价标准（24 个）</h2>
<blockquote>
<p>下列为仪表板实际监控的 24 个核心指标，按维度分组。<code>$instance</code> 为模板变量，实际查询均带 <code>instance=&quot;$instance&quot;</code> 过滤。</p>
</blockquote>
<h3 id="31-内存9-个">3.1 内存（9 个）</h3>
<table>
<thead>
<tr>
<th>指标</th>
<th>类型</th>
<th>含义</th>
<th>PromQL</th>
<th>评价标准</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>jvm_memory_bytes_used</code></td>
<td>gauge</td>
<td>已用内存（heap/nonheap）</td>
<td><code>jvm_memory_bytes_used{area=&quot;heap&quot;}</code></td>
<td>配合 max 看使用率，逼近上限即告警</td>
</tr>
<tr>
<td><code>jvm_memory_bytes_committed</code></td>
<td>gauge</td>
<td>JVM 已向 OS 申请的内存</td>
<td><code>jvm_memory_bytes_committed{area=&quot;heap&quot;}</code></td>
<td>committed 持续上涨 → 堆在扩张</td>
</tr>
<tr>
<td><code>jvm_memory_bytes_max</code></td>
<td>gauge</td>
<td>内存上限</td>
<td><code>jvm_memory_bytes_max{area=&quot;heap&quot;}</code></td>
<td>警戒线；Used→Max 距离越小越危险</td>
</tr>
<tr>
<td><code>jvm_memory_pool_bytes_used</code></td>
<td>gauge</td>
<td>各内存池已用（Eden/Old/Metaspace…）</td>
<td><code>jvm_memory_pool_bytes_used</code></td>
<td>Old 池逼近上限 → GC 压力前兆</td>
</tr>
<tr>
<td><code>jvm_memory_pool_bytes_max</code></td>
<td>gauge</td>
<td>各内存池上限</td>
<td><code>jvm_memory_pool_bytes_max</code></td>
<td>与 used 对比看各池余量</td>
</tr>
<tr>
<td><code>jvm_memory_pool_allocated_bytes_total</code></td>
<td>counter</td>
<td>累计分配字节数</td>
<td><code>rate(jvm_memory_pool_allocated_bytes_total[5m])</code></td>
<td>分配速率突增 → 可能触发频繁 GC</td>
</tr>
<tr>
<td><code>jvm_buffer_pool_used_bytes</code></td>
<td>gauge</td>
<td>直接内存缓冲池已用</td>
<td><code>jvm_buffer_pool_used_bytes</code></td>
<td>对比 capacity，看堆外内存</td>
</tr>
<tr>
<td><code>jvm_buffer_pool_capacity_bytes</code></td>
<td>gauge</td>
<td>缓冲池容量</td>
<td><code>jvm_buffer_pool_capacity_bytes</code></td>
<td>作为缓冲池上限警戒</td>
</tr>
<tr>
<td><code>vm_memory_heap_usage</code></td>
<td>gauge</td>
<td>堆使用率（0~1）</td>
<td><code>vm_memory_heap_usage</code></td>
<td><strong>&lt;0.8 健康 / 0.8~0.9 告警 / &gt;0.9 危险</strong></td>
</tr>
</tbody>
</table>
<h3 id="32-垃圾回收-gc2-个">3.2 垃圾回收 GC（2 个）</h3>
<table>
<thead>
<tr>
<th>指标</th>
<th>类型</th>
<th>含义</th>
<th>PromQL</th>
<th>评价标准</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>jvm_gc_collection_seconds_count</code></td>
<td>counter</td>
<td>GC 累计次数</td>
<td><code>rate(jvm_gc_collection_seconds_count[5m])</code></td>
<td>频率突升 = 频繁 GC，分配压力大</td>
</tr>
<tr>
<td><code>jvm_gc_collection_seconds_sum</code></td>
<td>counter</td>
<td>GC 累计停顿时长</td>
<td><code>rate(jvm_gc_collection_seconds_sum[5m])</code></td>
<td>停顿占比高 → 影响响应/吞吐</td>
</tr>
</tbody>
</table>
<blockquote>
<p>按 <code>gc</code> 标签区分收集器（如 G1 Young/Old），可分别看或汇总。</p>
</blockquote>
<h3 id="33-线程7-个">3.3 线程（7 个）</h3>
<table>
<thead>
<tr>
<th>指标</th>
<th>类型</th>
<th>含义</th>
<th>PromQL</th>
<th>评价标准</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>jvm_threads_current</code></td>
<td>gauge</td>
<td>活跃线程数</td>
<td><code>jvm_threads_current</code></td>
<td>与基线对比，突增/突降即异常</td>
</tr>
<tr>
<td><code>jvm_threads_daemon</code></td>
<td>gauge</td>
<td>守护线程数</td>
<td><code>jvm_threads_daemon</code></td>
<td>一般稳定，参考用</td>
</tr>
<tr>
<td><code>jvm_threads_peak</code></td>
<td>gauge</td>
<td>峰值线程数</td>
<td><code>jvm_threads_peak</code></td>
<td>参考最大并发承载</td>
</tr>
<tr>
<td><code>jvm_threads_state</code></td>
<td>gauge</td>
<td>各状态线程数</td>
<td><code>jvm_threads_state{state=&quot;BLOCKED&quot;}</code></td>
<td><strong>BLOCKED 高 = 锁竞争</strong>；WAITING 异常高 = 资源等待</td>
</tr>
<tr>
<td><code>jvm_threads_deadlocked</code></td>
<td>gauge</td>
<td>死锁线程数（循环依赖）</td>
<td><code>sum(jvm_threads_deadlocked or vector(0))</code></td>
<td><strong>&gt;0 即严重</strong></td>
</tr>
<tr>
<td><code>jvm_threads_deadlocked_monitor</code></td>
<td>gauge</td>
<td>死锁线程数（监视器）</td>
<td><code>sum(jvm_threads_deadlocked_monitor or vector(0))</code></td>
<td><strong>&gt;0 即严重</strong></td>
</tr>
<tr>
<td><code>jvm_threads_started_total</code></td>
<td>counter</td>
<td>累计启动线程数</td>
<td><code>rate(jvm_threads_started_total[5m])</code></td>
<td>创建速率持续高 = 线程池未复用/泄漏</td>
</tr>
</tbody>
</table>
<blockquote>
<p>死锁面板把两个死锁指标合并：<code>(sum(jvm_threads_deadlocked or vector(0)) + sum(jvm_threads_deadlocked_monitor or vector(0)))</code>。
注意：活跃线程数用 <code>jvm_threads_current</code>，不要用 <code>jvm_threads_live_threads</code>（重复命名，后者无数据）。</p>
</blockquote>
<h3 id="34-类加载3-个">3.4 类加载（3 个）</h3>
<table>
<thead>
<tr>
<th>指标</th>
<th>类型</th>
<th>含义</th>
<th>PromQL</th>
<th>评价标准</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>jvm_classes_currently_loaded</code></td>
<td>gauge</td>
<td>当前已加载类数</td>
<td><code>jvm_classes_currently_loaded</code></td>
<td>持续上涨 → 可能类/类加载器泄漏</td>
</tr>
<tr>
<td><code>jvm_classes_loaded_total</code></td>
<td>counter</td>
<td>累计加载类总数</td>
<td><code>jvm_classes_loaded_total</code></td>
<td>配合卸载数看加载 churn</td>
</tr>
<tr>
<td><code>jvm_classes_unloaded_total</code></td>
<td>counter</td>
<td>累计卸载类总数</td>
<td><code>jvm_classes_unloaded_total</code></td>
<td>卸载频繁 → 动态类/热部署，关注 Metaspace</td>
</tr>
</tbody>
</table>
<blockquote>
<p>注意区分：<code>jvm_classes_currently_loaded</code>（gauge，当前值）与 <code>jvm_classes_loaded_total</code>（counter，累计值）是两个不同指标，别混用。</p>
</blockquote>
<h3 id="35-运行时资源3-个--jvm_info">3.5 运行时资源（3 个 + jvm_info）</h3>
<table>
<thead>
<tr>
<th>指标</th>
<th>类型</th>
<th>含义</th>
<th>PromQL</th>
<th>评价标准</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>vm_cpu_load</code></td>
<td>gauge</td>
<td>JVM 进程 CPU 负载（0~1）</td>
<td><code>vm_cpu_load</code></td>
<td><strong>&lt;0.8 / 0.8~0.9 / &gt;0.9</strong></td>
</tr>
<tr>
<td><code>vm_file_descriptor_ratio</code></td>
<td>gauge</td>
<td>文件描述符使用率（0~1）</td>
<td><code>vm_file_descriptor_ratio</code></td>
<td>接近 1 → 可能打不开文件/连接</td>
</tr>
<tr>
<td><code>vm_uptime_milliseconds</code></td>
<td>gauge</td>
<td>进程运行时长</td>
<td><code>vm_uptime_milliseconds</code></td>
<td>参考稳定性；异常低 = 重启过</td>
</tr>
<tr>
<td><code>jvm_info</code></td>
<td>info</td>
<td>JVM 版本信息（常量 1）</td>
<td><code>jvm_info</code></td>
<td>不做图，仅用于 <code>label_values(jvm_info, instance)</code> 取实例列表</td>
</tr>
</tbody>
</table>
<blockquote>
<p><code>vm_memory_heap_usage</code> 既出现在内存区（timeseries 趋势）也出现在资源占比区（gauge 仪表），是同一指标两种展示。</p>
</blockquote>
<h3 id="36-端点全量指标说明未监控的为何不选">3.6 端点全量指标说明（未监控的为何不选）</h3>
<p>端点共 401 个指标名，除上述 24 个外，其余主要分四类，均<strong>刻意不选</strong>：</p>
<ul>
<li><strong>预聚合窗口变体</strong>：<code>vm_*_history</code> / <code>vm_*_window_5m</code> / <code>_15m</code> / <code>_1h</code> / <code>_x100</code>，与 Grafana <code>rate()</code> 重复。</li>
<li><strong>按池拆分变体</strong>：<code>vm_memory_pools_&lt;池名&gt;_*</code>（G1 Eden/Old/Survivor/Metaspace/CodeHeap…），已被 <code>jvm_memory_pool_bytes_*</code> 覆盖。</li>
<li><strong>重复命名</strong>：<code>jvm_threads_live_threads</code> / <code>jvm_memory_used_bytes</code> / <code>jvm_classes_loaded_classes</code> 等，与所选指标同义，选其一避免冗余。</li>
<li><strong>GC pause 细分</strong>：<code>jvm_gc_pause_seconds_*</code> / <code>jvm_gc_live_data_size_bytes</code> / <code>jvm_gc_max_data_size_bytes</code> / <code>jvm_gc_overhead_percent</code>，进阶 GC 分析用，本版未纳入，需要时可加。</li>
</ul>
<h2 id="四如何配置-prometheus-采集">四、如何配置 Prometheus 采集</h2>
<h3 id="41-jenkins-侧">4.1 Jenkins 侧</h3>
<ol>
<li>安装 <strong>Prometheus metrics plugin</strong>（<code>org.jenkins-ci.plugins:prometheus</code>）。</li>
<li>插件默认在 <code>http://&lt;jenkins&gt;/prometheus</code> 暴露指标（含 <code>jvm_*</code> 与 <code>vm_*</code>）。</li>
<li>（可选）在插件配置页调整暴露的指标范围；如需鉴权，配置允许的路径/认证。</li>
</ol>
<h3 id="42-prometheus-侧prometheusyml">4.2 Prometheus 侧（prometheus.yml）</h3>
<p>单实例：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span><span class="lnt">5
</span><span class="lnt">6
</span><span class="lnt">7
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="nt">scrape_configs</span><span class="p">:</span><span class="w">
</span><span class="w">  </span>- <span class="nt">job_name</span><span class="p">:</span><span class="w"> </span><span class="l">jenkins</span><span class="w">
</span><span class="w">    </span><span class="nt">metrics_path</span><span class="p">:</span><span class="w"> </span><span class="l">/prometheus</span><span class="w">
</span><span class="w">    </span><span class="nt">static_configs</span><span class="p">:</span><span class="w">
</span><span class="w">      </span>- <span class="nt">targets</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s1">&#39;jenkins-1:8080&#39;</span><span class="p">]</span><span class="w">
</span><span class="w">        </span><span class="nt">labels</span><span class="p">:</span><span class="w">
</span><span class="w">          </span><span class="nt">instance</span><span class="p">:</span><span class="w"> </span><span class="l">jks1</span><span class="w">
</span></code></pre></td></tr></table>
</div>
</div><p>多实例（同一 <code>metrics_path</code>，靠 <code>instance</code> label 区分）：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt"> 1
</span><span class="lnt"> 2
</span><span class="lnt"> 3
</span><span class="lnt"> 4
</span><span class="lnt"> 5
</span><span class="lnt"> 6
</span><span class="lnt"> 7
</span><span class="lnt"> 8
</span><span class="lnt"> 9
</span><span class="lnt">10
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="nt">scrape_configs</span><span class="p">:</span><span class="w">
</span><span class="w">  </span>- <span class="nt">job_name</span><span class="p">:</span><span class="w"> </span><span class="l">jenkins</span><span class="w">
</span><span class="w">    </span><span class="nt">metrics_path</span><span class="p">:</span><span class="w"> </span><span class="l">/prometheus</span><span class="w">
</span><span class="w">    </span><span class="nt">static_configs</span><span class="p">:</span><span class="w">
</span><span class="w">      </span>- <span class="nt">targets</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s1">&#39;jenkins-1:8080&#39;</span><span class="p">]</span><span class="w">
</span><span class="w">        </span><span class="nt">labels</span><span class="p">:</span><span class="w">
</span><span class="w">          </span><span class="nt">instance</span><span class="p">:</span><span class="w"> </span><span class="l">jks1</span><span class="w">
</span><span class="w">      </span>- <span class="nt">targets</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s1">&#39;jenkins-2:8080&#39;</span><span class="p">]</span><span class="w">
</span><span class="w">        </span><span class="nt">labels</span><span class="p">:</span><span class="w">
</span><span class="w">          </span><span class="nt">instance</span><span class="p">:</span><span class="w"> </span><span class="l">jks2</span><span class="w">
</span></code></pre></td></tr></table>
</div>
</div><blockquote>
<p>也可用 <code>relabel_configs</code> 把 <code>__address__</code> 重写为业务名做 <code>instance</code>。</p>
</blockquote>
<h3 id="43-验证采集">4.3 验证采集</h3>
<ul>
<li>Prometheus <code>/targets</code> → 对应 job <code>health=up</code></li>
<li><code>curl http://prometheus:9090/api/v1/query?query=up{job=&quot;jenkins&quot;}</code> → 值为 1</li>
<li><code>curl http://jenkins:8080/prometheus | grep '^jvm_'</code> → 看暴露了哪些指标名</li>
<li>Grafana 数据源代理：<code>/api/datasources/proxy/&lt;id&gt;/api/v1/query?query=...</code> 直接查某指标</li>
</ul>
<h2 id="五如何设计仪表板">五、如何设计仪表板</h2>
<h3 id="51-分区设计原则">5.1 分区设计原则</h3>
<ul>
<li><strong>按子系统分 row</strong>，从上到下：概览 → 内存 → GC → 线程 → 类加载 → 运行时 → 比率仪表 → 分布。</li>
<li><strong>信息密度递减</strong>：顶部是最重要的「总览快照」，下面是趋势细节与分布。</li>
<li>每个分区用 <code>row</code> 收纳，折叠/展开可控；分区标题用「中文(英文)」双语便于检索。</li>
</ul>
<p><strong>分区方案（9 个 row，24 个数据面板）：</strong></p>
<ol>
<li>JVM 概览 — 3 个 stat 单值快照（堆内存使用量、活跃线程数、死锁数）</li>
<li>内存使用 — 堆趋势 / 非堆趋势 / 内存池 / 分配速率（4 个 timeseries）</li>
<li>直接内存/缓冲池 — 缓冲池 Used vs Capacity（1 个 timeseries）</li>
<li>垃圾回收 — 频率 / 停顿（2 个 timeseries）</li>
<li>线程 — 数量 / 状态分布 / 创建速率（3 个 timeseries）</li>
<li>类加载 — 当前已加载 / 累计加载 / 累计卸载（3 个 timeseries）</li>
<li>虚拟机运行时 — CPU / 运行时长 / FD / 堆使用率（4 个 timeseries）</li>
<li>资源占比（Gauge） — 堆/FD/CPU 三个比率仪表（3 个 gauge）</li>
<li>线程状态分布（Bar gauge） — 各状态横向条形（1 个 bargauge）</li>
</ol>
<h3 id="52-panel-类型选择">5.2 Panel 类型选择</h3>
<table>
<thead>
<tr>
<th>类型</th>
<th>适用场景</th>
<th>本仪表板用法</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>stat</strong></td>
<td>单值快照，一眼知总体状态</td>
<td>概览区：堆内存使用量、活跃线程数、死锁数</td>
</tr>
<tr>
<td><strong>timeseries</strong></td>
<td>随时间变化的趋势，看异常时段</td>
<td>内存/GC/线程/类加载/运行时的所有趋势面板</td>
</tr>
<tr>
<td><strong>gauge</strong></td>
<td>比率/百分比，直观判断是否越线</td>
<td>CPU/FD/堆使用率（弧形 + 阈值着色）</td>
</tr>
<tr>
<td><strong>bargauge</strong></td>
<td>多类别占比对比</td>
<td>线程状态分布（各状态横向条形）</td>
</tr>
</tbody>
</table>
<blockquote>
<p>选型原则：状态量用 stat、趋势用 timeseries、比率用 gauge、分布用 bargauge。<strong>不为炫技堆面板</strong>，类型要与信息目的匹配。</p>
</blockquote>
<h3 id="53-布局24-列网格">5.3 布局（24 列网格）</h3>
<ul>
<li>全部面板基于 24 列网格（<code>gridPos</code>：x/y/w/h）。</li>
<li>概览区 stat：3 个并排，各占 <strong>8 宽</strong>，占满首行。</li>
<li>趋势 timeseries：<strong>12 宽</strong>（两个并排）或 <strong>24 宽</strong>（整行大图），高度 8。</li>
<li>比率 gauge：3 个并排各 8 宽，与概览区呼应。</li>
<li>bargauge：24 宽整行，展开各状态对比。</li>
<li>row 用 <code>h=1</code> 作分隔条；面板靠 <code>y</code> 顺序自上而下排（扁平 panels 列表，row 不嵌套子面板）。</li>
</ul>
<h3 id="54-视觉规范科技审美">5.4 视觉规范（科技审美）</h3>
<table>
<thead>
<tr>
<th>维度</th>
<th>设置</th>
<th>理由</th>
</tr>
</thead>
<tbody>
<tr>
<td>主题</td>
<td><code>dark</code> 深色</td>
<td>暗底高对比，长时间盯不累</td>
</tr>
<tr>
<td>十字线</td>
<td><code>graphTooltip=1</code> 共享</td>
<td>多图同时间对齐，定位异常时段</td>
</tr>
<tr>
<td>折线</td>
<td><code>lineWidth=2</code> + <code>fillOpacity=10</code> + <code>gradientMode=opacity</code> + <code>smooth</code> + <code>showPoints=never</code></td>
<td>淡填充 + 渐变，质感而非糊</td>
</tr>
<tr>
<td>图例</td>
<td>右侧、<code>calcs=[]</code>（无数值表）</td>
<td>不遮挡图形主体</td>
</tr>
<tr>
<td>Tooltip</td>
<td><code>mode=multi</code> + <code>sort=desc</code></td>
<td>多系列同时对比</td>
</tr>
<tr>
<td>阈值线</td>
<td>比率面板加 <code>thresholdsStyle=lines</code>，画 0.8 黄 / 0.9 红</td>
<td>与 gauge 阈值呼应，一眼看越线</td>
</tr>
<tr>
<td>上限线</td>
<td>内存趋势把 <code>Max</code> 系列设红色虚线</td>
<td>「Used 逼近上限」一目了然</td>
</tr>
<tr>
<td>配色</td>
<td>健康 <code>#00e676</code> / 告警 <code>#ffea00</code> / 危险 <code>#ff1744</code></td>
<td>全局统一三段阈值色</td>
</tr>
</tbody>
</table>
<blockquote>
<p>stat 单值面板配三段阈值 + 数值变色 + sparkline 迷你图，既有当前值又有近期走势。</p>
</blockquote>
<h2 id="六阈值--评价标准总表">六、阈值 / 评价标准总表</h2>
<table>
<thead>
<tr>
<th>维度</th>
<th>指标</th>
<th>健康(绿)</th>
<th>告警(黄)</th>
<th>危险(红)</th>
</tr>
</thead>
<tbody>
<tr>
<td>内存</td>
<td>堆使用率 <code>vm_memory_heap_usage</code></td>
<td>&lt;0.8</td>
<td>0.8~0.9</td>
<td>&gt;0.9</td>
</tr>
<tr>
<td>CPU</td>
<td><code>vm_cpu_load</code></td>
<td>&lt;0.8</td>
<td>0.8~0.9</td>
<td>&gt;0.9</td>
</tr>
<tr>
<td>文件描述符</td>
<td><code>vm_file_descriptor_ratio</code></td>
<td>&lt;0.8</td>
<td>0.8~0.9</td>
<td>&gt;0.9</td>
</tr>
<tr>
<td>死锁</td>
<td>deadlocked 计数</td>
<td>0</td>
<td>—</td>
<td>&gt;0</td>
</tr>
<tr>
<td>线程状态</td>
<td>BLOCKED 数</td>
<td>低</td>
<td>偏高</td>
<td>持续高</td>
</tr>
<tr>
<td>GC</td>
<td>频率/停顿</td>
<td>平稳</td>
<td>突升</td>
<td>持续高位</td>
</tr>
</tbody>
</table>
<blockquote>
<p>比率类阈值用 0~1（<code>percentunit</code>）；若 expr 显式 <code>*100</code>，则阈值改 80/90 并用 <code>unit=percent</code>。</p>
</blockquote>
<h2 id="七导入与维护">七、导入与维护</h2>
<ul>
<li><strong>导入方式</strong>：Grafana HTTP API <code>POST /api/dashboards/db</code>（带 <code>-d @file.json</code>）或 UI <code>+ → Import</code> 上传 JSON。JSON 外层须为 <code>{&quot;dashboard&quot;:{...}}</code>。</li>
<li><strong>覆盖更新</strong>：同 uid 覆盖会触发乐观锁，需先 <code>GET</code> 取当前 <code>version</code> 对齐 payload，或加 <code>?overwrite=true</code> 并先删旧版。</li>
<li><strong>模板变量</strong>：<code>$instance</code> 一处定义全局生效，新增 Jenkins target 无需改仪表板，下拉自动更新。</li>
<li><strong>扩展</strong>：要加新指标，遵循「先定维度→选类型→配评价标准→定布局」四步，保持分区与配色统一。</li>
<li><strong>仪表板源文件</strong>：已上传 GitHub <code>leoyim/jenkins-monitoring-dashboard</code>（<code>jenkins_jvm_monitor_v10.2.0.json</code>），可直接 import。</li>
</ul>
<h2 id="八监控效果">八、监控效果</h2>
<p></p>
<p></p>
<p></p>]]></description>
</item><item>
    <title>WSL2 Ubuntu 迁移实战</title>
    <link>http://leoyim.cn/wsl2-ubuntu-migration/</link>
    <pubDate>Wed, 17 Jun 2026 18:33:00 &#43;0800</pubDate>
    <author>作者</author>
    <guid>http://leoyim.cn/wsl2-ubuntu-migration/</guid>
    <description><![CDATA[<h2 id="前言">前言</h2>
<p>随着开发环境越来越复杂，WSL（Windows Subsystem for Linux）占用的空间也越来越大。</p>
<p>最初发现 WSL 占用了约 19GB 空间，而默认情况下 WSL2 的虚拟磁盘（<code>ext4.vhdx</code>）位于 C 盘：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-text" data-lang="text">C:\Users\&lt;用户名&gt;\AppData\Local\Packages\...
</code></pre></td></tr></table>
</div>
</div><p>对于系统盘容量有限的开发机器来说，将 WSL 迁移到其他磁盘（如 E 盘）是一个非常合理的选择。</p>
<p>本文记录一次完整的迁移过程，包括：</p>
<ul>
<li>导出 Ubuntu</li>
<li>注销旧发行版</li>
<li>导入到 E 盘</li>
<li>验证环境</li>
<li>解决常见报错</li>
<li>保证原有开发环境可继续使用</li>
</ul>
<hr>
<h2 id="为什么迁移-wsl">为什么迁移 WSL</h2>
<p>WSL2 实际上运行在一个虚拟磁盘中：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-text" data-lang="text">ext4.vhdx
</code></pre></td></tr></table>
</div>
</div><p>常见占用来源：</p>
<ul>
<li>Ubuntu 系统本身</li>
<li>Python 虚拟环境</li>
<li>Node.js 项目</li>
<li>Docker 镜像</li>
<li>数据库文件</li>
<li>AI 模型</li>
</ul>
<p>19GB 对于开发环境来说属于正常范围。</p>
<p>但是：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-text" data-lang="text">C盘容量有限
E盘容量充足
</code></pre></td></tr></table>
</div>
</div><p>因此决定迁移。</p>
<hr>
<h2 id="查看当前-wsl-状态">查看当前 WSL 状态</h2>
<p>首先查看当前发行版：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-powershell" data-lang="powershell"><span class="n">wsl</span> <span class="p">-</span><span class="n">-list</span> <span class="p">-</span><span class="n">-verbose</span>
</code></pre></td></tr></table>
</div>
</div><p>例如：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-text" data-lang="text">NAME      STATE    VERSION
Ubuntu    Running  2
</code></pre></td></tr></table>
</div>
</div><p>确认：</p>
<ul>
<li>使用的是 WSL2</li>
<li>当前发行版名称为 Ubuntu</li>
</ul>
<hr>
<h2 id="第一步关闭-wsl">第一步：关闭 WSL</h2>
<p>迁移前关闭所有实例：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-powershell" data-lang="powershell"><span class="n">wsl</span> <span class="p">-</span><span class="n">-shutdown</span>
</code></pre></td></tr></table>
</div>
</div><hr>
<h2 id="第二步导出-ubuntu">第二步：导出 Ubuntu</h2>
<p>创建备份目录：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-powershell" data-lang="powershell"><span class="n">mkdir</span> <span class="n">E:</span><span class="p">\</span><span class="n">WSL_Backup</span>
</code></pre></td></tr></table>
</div>
</div><p>导出发行版：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-powershell" data-lang="powershell"><span class="n">wsl</span> <span class="p">-</span><span class="n">-export</span> <span class="n">Ubuntu</span> <span class="n">E:</span><span class="p">\</span><span class="n">WSL_Backup</span><span class="p">\</span><span class="n">Ubuntu</span><span class="p">-</span><span class="n">2026</span><span class="p">-</span><span class="n">06</span><span class="p">-</span><span class="n">17</span><span class="p">.</span><span class="n">tar</span>
</code></pre></td></tr></table>
</div>
</div><p>导出完成后确认文件存在：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-powershell" data-lang="powershell"><span class="nb">dir </span><span class="n">E:</span><span class="p">\</span><span class="n">WSL_Backup</span>
</code></pre></td></tr></table>
</div>
</div><p>应该看到：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-text" data-lang="text">Ubuntu-2026-06-17.tar
</code></pre></td></tr></table>
</div>
</div><hr>
<h2 id="第三步注销旧发行版">第三步：注销旧发行版</h2>
<p>确认备份成功后：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-powershell" data-lang="powershell"><span class="n">wsl</span> <span class="p">-</span><span class="n">-unregister</span> <span class="n">Ubuntu</span>
</code></pre></td></tr></table>
</div>
</div><p>此操作会：</p>
<ul>
<li>删除原来的发行版注册信息</li>
<li>删除 C 盘中的 <code>ext4.vhdx</code></li>
</ul>
<p>此时执行：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-powershell" data-lang="powershell"><span class="n">wsl</span> <span class="n">-l</span> <span class="n">-v</span>
</code></pre></td></tr></table>
</div>
</div><p>Ubuntu 应该已经消失。</p>
<hr>
<h2 id="第四步创建新的安装目录">第四步：创建新的安装目录</h2>
<p>例如：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-powershell" data-lang="powershell"><span class="n">mkdir</span> <span class="n">E:</span><span class="p">\</span><span class="n">WSL</span><span class="p">\</span><span class="n">Ubuntu</span>
</code></pre></td></tr></table>
</div>
</div><p>目录结构：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span><span class="lnt">5
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-text" data-lang="text">E:
├─WSL
│  └─Ubuntu
└─WSL_Backup
   └─Ubuntu-2026-06-17.tar
</code></pre></td></tr></table>
</div>
</div><hr>
<h2 id="第五步重新导入到-e-盘">第五步：重新导入到 E 盘</h2>
<p>这里是最关键的一步。</p>
<p>正确命令：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-powershell" data-lang="powershell"><span class="n">wsl</span> <span class="p">-</span><span class="n">-import</span> <span class="n">Ubuntu</span> <span class="n">E:</span><span class="p">\</span><span class="n">WSL</span><span class="p">\</span><span class="n">Ubuntu</span> <span class="n">E:</span><span class="p">\</span><span class="n">WSL_Backup</span><span class="p">\</span><span class="n">Ubuntu</span><span class="p">-</span><span class="n">2026</span><span class="p">-</span><span class="n">06</span><span class="p">-</span><span class="n">17</span><span class="p">.</span><span class="n">tar</span> <span class="p">-</span><span class="n">-version</span> <span class="n">2</span>
</code></pre></td></tr></table>
</div>
</div><p>参数说明：</p>
<table>
<thead>
<tr>
<th>参数</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>Ubuntu</code></td>
<td>发行版名称</td>
</tr>
<tr>
<td><code>E:\WSL\Ubuntu</code></td>
<td>安装目录</td>
</tr>
<tr>
<td><code>Ubuntu-2026-06-17.tar</code></td>
<td>备份文件</td>
</tr>
<tr>
<td><code>--version 2</code></td>
<td>指定使用 WSL2</td>
</tr>
</tbody>
</table>
<hr>
<h2 id="常见错误wsle_invalidarg">常见错误：Wsl/E_INVALIDARG</h2>
<p>错误示例：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-text" data-lang="text">参数错误
错误代码: Wsl/E_INVALIDARG
</code></pre></td></tr></table>
</div>
</div><p>原因：命令格式错误。</p>
<p>例如：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-powershell" data-lang="powershell"><span class="n">wsl</span> <span class="p">-</span><span class="n">-import</span> <span class="n">Ubuntu</span> <span class="n">E:</span><span class="p">\</span><span class="n">WSL_Backup</span><span class="p">\</span><span class="n">Ubuntu</span><span class="p">-</span><span class="n">2026</span><span class="p">-</span><span class="n">06</span><span class="p">-</span><span class="n">17</span><span class="p">.</span><span class="n">tar</span> <span class="p">-</span><span class="n">-version</span> <span class="n">2</span>
</code></pre></td></tr></table>
</div>
</div><p>少了安装目录参数。</p>
<p>正确格式必须是：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-powershell" data-lang="powershell"><span class="n">wsl</span> <span class="p">-</span><span class="n">-import</span> <span class="n">名称</span> <span class="n">安装目录</span> <span class="n">tar文件</span>
</code></pre></td></tr></table>
</div>
</div><hr>
<h2 id="验证迁移是否成功">验证迁移是否成功</h2>
<p>查看发行版：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-powershell" data-lang="powershell"><span class="n">wsl</span> <span class="n">-l</span> <span class="n">-v</span>
</code></pre></td></tr></table>
</div>
</div><p>例如：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-text" data-lang="text">NAME      STATE    VERSION
Ubuntu    Stopped  2
</code></pre></td></tr></table>
</div>
</div><p>启动：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-powershell" data-lang="powershell"><span class="n">wsl</span> <span class="n">-d</span> <span class="n">Ubuntu</span>
</code></pre></td></tr></table>
</div>
</div><p>进入系统后：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">whoami
<span class="nb">pwd</span>
</code></pre></td></tr></table>
</div>
</div><p>如果能够正常进入：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-text" data-lang="text">/home/&lt;user&gt;
</code></pre></td></tr></table>
</div>
</div><p>说明系统已经恢复。</p>
<hr>
<h2 id="常见错误wsl_e_distro_not_found">常见错误：WSL_E_DISTRO_NOT_FOUND</h2>
<p>错误示例：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-text" data-lang="text">不存在具有所提供名称的分发

WSL_E_DISTRO_NOT_FOUND
</code></pre></td></tr></table>
</div>
</div><p>出现原因通常是：</p>
<ul>
<li>Windows Terminal 保存了旧配置</li>
<li>原 Ubuntu 已注销</li>
<li>新 Ubuntu 已重新导入</li>
</ul>
<p>此时：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-powershell" data-lang="powershell"><span class="n">wsl</span> <span class="n">-d</span> <span class="n">Ubuntu</span>
</code></pre></td></tr></table>
</div>
</div><p>如果能够正常进入系统，说明 WSL 本身没问题，仅仅是 Terminal 配置失效。</p>
<p>解决办法：删除旧 Profile，重新创建：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-text" data-lang="text">wsl.exe -d Ubuntu
</code></pre></td></tr></table>
</div>
</div><p>即可。</p>
<hr>
<h2 id="常见错误wsl_e_not_a_linux_distro">常见错误：WSL_E_NOT_A_LINUX_DISTRO</h2>
<p>错误示例：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-text" data-lang="text">WSL_E_NOT_A_LINUX_DISTRO
</code></pre></td></tr></table>
</div>
</div><p>原因：导入的 tar 文件并不是 Linux RootFS。</p>
<p>WSL 要求导入的内容必须包含：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-text" data-lang="text">/etc
/usr
/bin
/lib
</code></pre></td></tr></table>
</div>
</div><p>检查方法：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-powershell" data-lang="powershell"><span class="n">tar</span> <span class="n">-tf</span> <span class="n">Ubuntu</span><span class="p">.</span><span class="n">tar</span>
</code></pre></td></tr></table>
</div>
</div><p>正常情况应看到：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-text" data-lang="text">etc/
usr/
bin/
lib/
</code></pre></td></tr></table>
</div>
</div><p>如果看到：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-text" data-lang="text">Users/
Desktop/
Documents/
</code></pre></td></tr></table>
</div>
</div><p>说明导出的文件不是 Linux 根文件系统，需要重新导出。</p>
<hr>
<h2 id="如何确认数据已经迁移到-e-盘">如何确认数据已经迁移到 E 盘</h2>
<p>关闭 WSL：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-powershell" data-lang="powershell"><span class="n">wsl</span> <span class="p">-</span><span class="n">-shutdown</span>
</code></pre></td></tr></table>
</div>
</div><p>查看：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-powershell" data-lang="powershell"><span class="nb">dir </span><span class="n">E:</span><span class="p">\</span><span class="n">WSL</span><span class="p">\</span><span class="n">Ubuntu</span>
</code></pre></td></tr></table>
</div>
</div><p>应该能看到：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-text" data-lang="text">ext4.vhdx
</code></pre></td></tr></table>
</div>
</div><p>例如：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-text" data-lang="text">ext4.vhdx 19GB
</code></pre></td></tr></table>
</div>
</div><p>说明 Linux 文件系统已经位于 E 盘。</p>
<hr>
<h2 id="验证开发环境是否完整">验证开发环境是否完整</h2>
<p>迁移完成后建议检查：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">python3 --version
node --version
git --version
docker --version
</code></pre></td></tr></table>
</div>
</div><p>以及：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">ls ~
</code></pre></td></tr></table>
</div>
</div><p>确认：</p>
<ul>
<li>项目代码存在</li>
<li>Python 环境存在</li>
<li>Node 环境存在</li>
<li>Git 配置正常</li>
</ul>
<hr>
<h2 id="docker-用户特别注意">Docker 用户特别注意</h2>
<p>如果安装了 Docker Desktop，执行：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-powershell" data-lang="powershell"><span class="n">wsl</span> <span class="n">-l</span> <span class="n">-v</span>
</code></pre></td></tr></table>
</div>
</div><p>可能看到：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-text" data-lang="text">docker-desktop
docker-desktop-data
</code></pre></td></tr></table>
</div>
</div><p>很多时候真正占空间的是：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-text" data-lang="text">docker-desktop-data
</code></pre></td></tr></table>
</div>
</div><p>其占用甚至可能超过 Ubuntu 本身。</p>
<p>迁移 Ubuntu 不会自动迁移 Docker 数据，需要单独处理 Docker 存储位置。</p>
<hr>
<h2 id="迁移后的目录建议">迁移后的目录建议</h2>
<p>推荐结构：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span><span class="lnt">5
</span><span class="lnt">6
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-text" data-lang="text">E:
└─WSL
   ├─Ubuntu
   ├─Debian
   ├─Arch
   └─Docker
</code></pre></td></tr></table>
</div>
</div><p>方便后续管理多个 Linux 环境。</p>
<hr>
<h2 id="总结">总结</h2>
<p>本次迁移的核心步骤：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span><span class="lnt">5
</span><span class="lnt">6
</span><span class="lnt">7
</span><span class="lnt">8
</span><span class="lnt">9
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-powershell" data-lang="powershell"><span class="n">wsl</span> <span class="p">-</span><span class="n">-shutdown</span>

<span class="n">wsl</span> <span class="p">-</span><span class="n">-export</span> <span class="n">Ubuntu</span> <span class="n">E:</span><span class="p">\</span><span class="n">WSL_Backup</span><span class="p">\</span><span class="n">Ubuntu</span><span class="p">.</span><span class="n">tar</span>

<span class="n">wsl</span> <span class="p">-</span><span class="n">-unregister</span> <span class="n">Ubuntu</span>

<span class="n">mkdir</span> <span class="n">E:</span><span class="p">\</span><span class="n">WSL</span><span class="p">\</span><span class="n">Ubuntu</span>

<span class="n">wsl</span> <span class="p">-</span><span class="n">-import</span> <span class="n">Ubuntu</span> <span class="n">E:</span><span class="p">\</span><span class="n">WSL</span><span class="p">\</span><span class="n">Ubuntu</span> <span class="n">E:</span><span class="p">\</span><span class="n">WSL_Backup</span><span class="p">\</span><span class="n">Ubuntu</span><span class="p">.</span><span class="n">tar</span> <span class="p">-</span><span class="n">-version</span> <span class="n">2</span>
</code></pre></td></tr></table>
</div>
</div><p>验证：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-powershell" data-lang="powershell"><span class="n">wsl</span> <span class="n">-d</span> <span class="n">Ubuntu</span>
</code></pre></td></tr></table>
</div>
</div><p>如果能够正常进入系统，并且：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-text" data-lang="text">E:\WSL\Ubuntu\ext4.vhdx
</code></pre></td></tr></table>
</div>
</div><p>已经生成，则说明迁移成功。</p>
<p>最终结果：</p>
<ul>
<li>释放 C 盘空间</li>
<li>保留原有 Linux 环境</li>
<li>保留开发工具和项目</li>
<li>保持 VS Code、Docker、Python 等工作流不变</li>
</ul>
<p>对于长期使用 WSL 进行开发的用户，这是最稳妥、最推荐的迁移方案。</p>]]></description>
</item><item>
    <title>Linux自启动服务配置和启用</title>
    <link>http://leoyim.cn/linux-service-configure-enable/</link>
    <pubDate>Thu, 19 Sep 2024 09:13:03 &#43;0800</pubDate>
    <author>作者</author>
    <guid>http://leoyim.cn/linux-service-configure-enable/</guid>
    <description><![CDATA[<blockquote>
<p>本文使用以下Go项目作为配置示例，编译后获得可执行程序<code>main</code></p>
</blockquote>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt"> 1
</span><span class="lnt"> 2
</span><span class="lnt"> 3
</span><span class="lnt"> 4
</span><span class="lnt"> 5
</span><span class="lnt"> 6
</span><span class="lnt"> 7
</span><span class="lnt"> 8
</span><span class="lnt"> 9
</span><span class="lnt">10
</span><span class="lnt">11
</span><span class="lnt">12
</span><span class="lnt">13
</span><span class="lnt">14
</span><span class="lnt">15
</span><span class="lnt">16
</span><span class="lnt">17
</span><span class="lnt">18
</span><span class="lnt">19
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-go" data-lang="go"><span class="kn">package</span> <span class="nx">main</span>

<span class="kn">import</span> <span class="p">(</span>
	<span class="s">&#34;net/http&#34;</span>

	<span class="s">&#34;github.com/gin-gonic/gin&#34;</span>
<span class="p">)</span>

<span class="kd">func</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
	<span class="nx">r</span> <span class="o">:=</span> <span class="nx">gin</span><span class="p">.</span><span class="nf">Default</span><span class="p">()</span>

	<span class="nx">r</span><span class="p">.</span><span class="nf">GET</span><span class="p">(</span><span class="s">&#34;/hello&#34;</span><span class="p">,</span> <span class="nx">hello</span><span class="p">)</span>

	<span class="nx">r</span><span class="p">.</span><span class="nf">Run</span><span class="p">(</span><span class="s">&#34;:8080&#34;</span><span class="p">)</span>
<span class="p">}</span>

<span class="kd">func</span> <span class="nf">hello</span><span class="p">(</span><span class="nx">c</span> <span class="o">*</span><span class="nx">gin</span><span class="p">.</span><span class="nx">Context</span><span class="p">)</span> <span class="p">{</span>
	<span class="nx">c</span><span class="p">.</span><span class="nf">JSON</span><span class="p">(</span><span class="nx">http</span><span class="p">.</span><span class="nx">StatusOK</span><span class="p">,</span> <span class="nx">gin</span><span class="p">.</span><span class="nx">H</span><span class="p">{</span><span class="s">&#34;[MESAGE]&#34;</span><span class="p">:</span> <span class="s">&#34;Hello World!&#34;</span><span class="p">})</span>
<span class="p">}</span>
</code></pre></td></tr></table>
</div>
</div><blockquote>
<p>执行可执行程序后，可在本地访问<code>http://localhost:8080/hello</code>接口，如下所示：</p>
</blockquote>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="o">[</span>root@VM-12-5-centos ~<span class="o">]</span><span class="c1"># curl http://localhost:8080/hello</span>
<span class="o">{</span><span class="s2">&#34;[MESAGE]&#34;</span>:<span class="s2">&#34;Hello World!&#34;</span><span class="o">}</span>
</code></pre></td></tr></table>
</div>
</div><p>在 Linux 系统中，可以使用 systemd 管理自启动服务。以下是配置和启用自启动服务的基本步骤：</p>
<h2 id="1-创建服务文件">1 创建服务文件</h2>
<p>服务文件一般位于 /etc/systemd/system/ 目录下，文件名以 .service 结尾</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">vi /etc/systemd/system/my_service.service
</code></pre></td></tr></table>
</div>
</div><p>在文件中添加以下内容：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt"> 1
</span><span class="lnt"> 2
</span><span class="lnt"> 3
</span><span class="lnt"> 4
</span><span class="lnt"> 5
</span><span class="lnt"> 6
</span><span class="lnt"> 7
</span><span class="lnt"> 8
</span><span class="lnt"> 9
</span><span class="lnt">10
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-ini" data-lang="ini"><span class="k">[Unit]</span>
<span class="na">Description</span><span class="o">=</span><span class="s">My Custom Service</span>
<span class="na">After</span><span class="o">=</span><span class="s">network.target</span>

<span class="k">[Service]</span>
<span class="na">ExecStart</span><span class="o">=</span><span class="s">/path/to/your/executable_or_script</span>
<span class="na">Restart</span><span class="o">=</span><span class="s">on-failure</span>

<span class="k">[Install]</span>
<span class="na">WantedBy</span><span class="o">=</span><span class="s">multi-user.target</span>
</code></pre></td></tr></table>
</div>
</div><blockquote>
<p>Description：对服务的描述。
After：指定服务应在什么条件下启动（例如 network.target 表示在网络服务启动后）。
ExecStart：定义服务启动时执行的命令（可以是可执行文件或脚本）。
Restart：定义服务在失败时是否自动重启（可选项）。</p>
</blockquote>
<h2 id="2-重新加载-systemd-配置">2 重新加载 systemd 配置</h2>
<p>添加或修改 .service 文件后，重新加载 systemd 配置：</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">sudo systemctl daemon-reload
</code></pre></td></tr></table>
</div>
</div><h2 id="3-手动启动服务">3 手动启动服务</h2>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">sudo systemctl start my_service.service
</code></pre></td></tr></table>
</div>
</div><h2 id="4-启用服务">4 启用服务</h2>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">sudo systemctl <span class="nb">enable</span> my_service.service
</code></pre></td></tr></table>
</div>
</div><h2 id="5-查看服务状态">5 查看服务状态</h2>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">sudo systemctl status my_service.service
</code></pre></td></tr></table>
</div>
</div><h2 id="6-停止服务">6 停止服务</h2>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">sudo systemctl stop my_service.service
</code></pre></td></tr></table>
</div>
</div><h2 id="7-禁用服务">7 禁用服务</h2>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">sudo systemctl disable my_service.service
</code></pre></td></tr></table>
</div>
</div>]]></description>
</item><item>
    <title>Docker备忘清单</title>
    <link>http://leoyim.cn/docker-cheat-sheet/</link>
    <pubDate>Fri, 06 Sep 2024 22:10:40 &#43;0800</pubDate>
    <author>作者</author>
    <guid>http://leoyim.cn/docker-cheat-sheet/</guid>
    <description><![CDATA[<p>本文记录了Docker的常用命令</p>
<ol>
<li>
<p>获取 Docker 帮助信息</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell">docker --help
</code></pre></td></tr></table>
</div>
</div></li>
<li>
<p>获取 Docker 版本号</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell">docker version
</code></pre></td></tr></table>
</div>
</div></li>
<li>
<p>从远程仓库下载指定的镜像到本地</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell">docker pull &lt;image&gt;
</code></pre></td></tr></table>
</div>
</div></li>
<li>
<p>根据 Dockerfile 构建一个新的镜像</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback">docker build -t &lt;image&gt; -f &lt;dockerfile&gt; .
</code></pre></td></tr></table>
</div>
</div></li>
<li>
<p>列出本地已下载的镜像列表</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell">docker images
</code></pre></td></tr></table>
</div>
</div></li>
<li>
<p>列出正在运行的容器</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell">docker ps
<span class="c1"># 列出所有容器（包括已停止的容器）</span>
docker ps -a
</code></pre></td></tr></table>
</div>
</div></li>
<li>
<p>启动一个已停止的容器</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell">docker start &lt;container&gt;
</code></pre></td></tr></table>
</div>
</div></li>
<li>
<p>停止一个正在运行的容器</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback">docker stop &lt;container&gt;
</code></pre></td></tr></table>
</div>
</div></li>
<li>
<p>重启一个容器</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell">docker restart &lt;container&gt;
</code></pre></td></tr></table>
</div>
</div></li>
<li>
<p>在正在运行的容器中执行命令</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell">docker <span class="nb">exec</span> &lt;container&gt; &lt;command&gt;
</code></pre></td></tr></table>
</div>
</div></li>
<li>
<p>删除指定的容器</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell">docker rm &lt;container&gt;
</code></pre></td></tr></table>
</div>
</div></li>
<li>
<p>删除指定的镜像名</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell">docker rmi &lt;image&gt;
</code></pre></td></tr></table>
</div>
</div></li>
<li>
<p>将镜像转换为压缩文件</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell">docker save -o &lt;tar&gt; &lt;image&gt;
</code></pre></td></tr></table>
</div>
</div></li>
<li>
<p>将压缩文件转换为镜像</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell">docker load -i &lt;tar&gt;
</code></pre></td></tr></table>
</div>
</div></li>
<li>
<p>将容器快照保存为新的镜像</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell">docker commit &lt;container&gt; &lt;image&gt;
</code></pre></td></tr></table>
</div>
</div></li>
<li>
<p>获取Docker对象(容器、镜像、网络、数据卷等)的低级配置和状态信息</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell">docker inspect &lt;image&gt;<span class="p">|</span>&lt;container&gt;<span class="p">|</span>&lt;volume&gt;<span class="p">|</span>&lt;network&gt;
</code></pre></td></tr></table>
</div>
</div></li>
<li>
<p>用于在容器与主机之间的复制文件或目录</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell"><span class="c1"># 容器 -&gt; 主机</span>
docker cp &lt;container&gt;:&lt;containerfilePath&gt; &lt;localFilePath&gt;
<span class="c1"># 主机 -&gt; 容器</span>
docker cp &lt;localFilePath&gt; &lt;container&gt;:&lt;containerfilePath&gt;
</code></pre></td></tr></table>
</div>
</div></li>
<li>
<p>显示Docker主机系统相关信息</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell">docker system info
</code></pre></td></tr></table>
</div>
</div></li>
<li>
<p>显示Docker使用空间情况的系统报告</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell">docker system df
</code></pre></td></tr></table>
</div>
</div></li>
<li>
<p>查找Docker Hub上的镜像</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell">docker search ubuntu
</code></pre></td></tr></table>
</div>
</div></li>
<li>
<p>查看容器日志</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell">docker logs &lt;container&gt;
</code></pre></td></tr></table>
</div>
</div></li>
<li>
<p>给镜像创建一个标签</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell">docker tag &lt;image&gt; &lt;tag&gt;
</code></pre></td></tr></table>
</div>
</div></li>
<li>
<p>运行一个镜像</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-shell" data-lang="shell">docker run -name &lt;container&gt; &lt;image&gt;
</code></pre></td></tr></table>
</div>
</div></li>
</ol>]]></description>
</item><item>
    <title>通过Kubeconfig文件生成X509证书</title>
    <link>http://leoyim.cn/based-kubeconfig-generic-x509-certificate/</link>
    <pubDate>Fri, 12 Jul 2024 09:55:25 &#43;0800</pubDate>
    <author>作者</author>
    <guid>http://leoyim.cn/based-kubeconfig-generic-x509-certificate/</guid>
    <description><![CDATA[<p>本文记录了从Kubernetes集群kubeconfig.yaml文件提取生成X509证书的方法。</p>
<p>题图源：<a href="https://blog.stackademic.com/kubernetes-certificate-expiration-x509-792e48175967" target="_blank" rel="noopener noreffer">Kubernetes certificate expiration “X509”</a></p>
<hr>
<h2 id="1-查看kubeconfigyaml">1 查看kubeconfig.yaml</h2>
<blockquote>
<p>可以使用VSCode等文本编辑器打开kubeconfig.yaml文件，或者在终端使用cat, less或more命令查看具体内容。</p>
</blockquote>
<p>kubeconfig文件的结构通常包含一个或多个clusters部分，每个部分里有cluster、user和contexts等字段。X509证书通常存储在clusters下的cluster段落中的certificate-authority-data字段和users下的某个用户段落中的client-certificate-data及client-key-data字段，这些字段用于存储kubectl客户端与Kubernetes API服务器进行身份验证所需的凭据。</p>
<ul>
<li><strong>client-key</strong> 此字段存储客户端证书的私钥。客户端证书用于将 <code>kubectl</code>客户端验证到API服务器。私钥必须保密，因为它可用于冒充客户端。</li>
<li><strong>client-certificate</strong> 此字段以PEM格式存储客户端证书。客户端证书用于将 <code>kubectl</code>客户端验证到API服务器。证书必须由受信任的证书颁发机构(CA)签名。</li>
<li><strong>cluster-ca-certificate</strong> 此字段以PEM格式存储API服务器的CA证书。CA证书用于验证API服务器证书的真实性。CA 证书必须受kubectl客户端信任。</li>
</ul>
<div class="details admonition note open">
        <div class="details-summary admonition-title">
            <i class="icon fas fa-pencil-alt fa-fw"></i>注意<i class="details-icon fas fa-angle-right fa-fw"></i>
        </div>
        <div class="details-content">
            <div class="admonition-content">client-key 和 client-certificate 字段应保密。cluster-ca-certificate 字段可以与他人共享，用于验证API服务器证书的真实性。</div>
        </div>
    </div>
<h2 id="2-提取证书和密钥">2 提取证书和密钥</h2>
<blockquote>
<p>证书数据是以Base64编码的形式存储的。你可以使用以下命令来解码这些数据并将其保存为PEM文件：</p>
</blockquote>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span><span class="lnt">5
</span><span class="lnt">6
</span><span class="lnt">7
</span><span class="lnt">8
</span><span class="lnt">9
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-sh" data-lang="sh"><span class="c1"># 提取集群CA证书到ca.crt</span>
<span class="nb">echo</span> <span class="s2">&#34;</span><span class="k">$(</span>base64 -d <span class="o">&lt;&lt;&lt;</span> <span class="k">$(</span>grep <span class="s1">&#39;certificate-authority-data&#39;</span> kubeconfig.yaml <span class="p">|</span> awk <span class="s1">&#39;{print $2}&#39;</span><span class="k">))</span><span class="s2">&#34;</span> &gt; ca.crt

<span class="c1"># 提取客户端证书到client.crt</span>
<span class="nb">echo</span> <span class="s2">&#34;</span><span class="k">$(</span>base64 -d <span class="o">&lt;&lt;&lt;</span> <span class="k">$(</span>grep <span class="s1">&#39;client-certificate-data&#39;</span> kubeconfig.yaml <span class="p">|</span> awk <span class="s1">&#39;{print $2}&#39;</span><span class="k">))</span><span class="s2">&#34;</span> &gt; client.crt

<span class="c1"># 提取客户端私钥到client.key</span>
<span class="nb">echo</span> <span class="s2">&#34;</span><span class="k">$(</span>base64 -d <span class="o">&lt;&lt;&lt;</span> <span class="k">$(</span>grep <span class="s1">&#39;client-key-data&#39;</span> kubeconfig.yaml <span class="p">|</span> awk <span class="s1">&#39;{print $2}&#39;</span><span class="k">))</span><span class="s2">&#34;</span> &gt; client.key

</code></pre></td></tr></table>
</div>
</div><h2 id="3-验证证书">3 验证证书</h2>
<p>解码并保存后，你可以使用openssl命令行工具来验证生成的证书和私钥是否有效，这个命令会显示客户端证书的详细信息，包括有效期等。</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-sh" data-lang="sh">openssl x509 -in client.crt -text -noout
</code></pre></td></tr></table>
</div>
</div>]]></description>
</item><item>
    <title>Kubernetes集群搭建部署实践</title>
    <link>http://leoyim.cn/deploy-kubernetes-cluster-locally/</link>
    <pubDate>Sat, 18 Nov 2023 10:13:11 &#43;0800</pubDate>
    <author>作者</author>
    <guid>http://leoyim.cn/deploy-kubernetes-cluster-locally/</guid>
    <description><![CDATA[<p>本文记录了在作者本地使用三台CentOS虚拟机，搭建部署单Master双Slave Kubernetes集群。</p>
<p>题图源：<a href="https://www.kosli.com/blog/understanding-your-kubernetes-deployment-lifecyclea-guide-with-examples-1/" target="_blank" rel="noopener noreffer">Understanding Your Kubernetes Deployment Lifecycle</a></p>
<hr>
<h2 id="1-系统准备">1 系统准备</h2>
<p>搭建部署包含1个Master结点和2个Slave结点的Kubernetes集群，需要准备3台Linux虚拟机作为集群结点，可以通过VMware Worksation快速创建三台CentOS虚拟机。</p>
<h3 id="11-工具安装">1.1 工具安装</h3>
<ul>
<li><i class="far fa-check-square fa-fw"></i> <a href="https://www.vmware.com/cn/products/workstation-pro.html" target="_blank" rel="noopener noreffer">WMware Workstation Pro 17</a></li>
<li><i class="far fa-check-square fa-fw"></i> <a href="https://mirrors.tuna.tsinghua.edu.cn/centos/7.9.2009/isos/x86_64/CentOS-7-x86_64-Minimal-2009.iso" target="_blank" rel="noopener noreffer">CentOS-7-x86_64-Minimal-2009.iso</a></li>
</ul>
<h3 id="12-网络配置">1.2 网络配置</h3>
<p>使用VMware工具本身的界面操作起来并不方便，对CentOS虚拟机固定IP后，可使虚拟机每次启动时IP不会发生变化，可以使用<code>XShell</code>或<code>Tabby</code>通过SSH访问虚拟机，进行后续的配置操作，会节省很多时间。</p>
<p>此配置保证后续Kubernetes集群结点IP不会随意改变，这里不做过多介绍，具体可参考这篇文章：<a href="https://blog.csdn.net/jsryin/article/details/123304582" target="_blank" rel="noopener noreffer">Vmware虚拟机Linux配置固定IP地址（详细版）</a>。</p>
<p>通过以上配置，在各个结点执行<code>ip addr</code>获取到三个结点的IP信息：</p>
<blockquote>
<p>192.168.198.129<br>
192.168.198.130<br>
192.168.198.131</p>
</blockquote>
<h3 id="13-centos系统配置">1.3 CentOS系统配置</h3>
<h4 id="131-主机名配置">1.3.1 主机名配置</h4>
<p>设置192.168.198.129主机名 apple</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">hostnamectl set-hostname apple
</code></pre></td></tr></table>
</div>
</div><p>设置192.168.198.130主机名 banana</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">hostnamectl set-hostname banana
</code></pre></td></tr></table>
</div>
</div><p>设置192.168.198.131主机名 cherry</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">hostnamectl set-hostname cherry
</code></pre></td></tr></table>
</div>
</div><h4 id="132-配置主机ip地址">1.3.2 配置主机IP地址</h4>
<p>设置Apple IP地址为 192.168.198.129</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt"> 1
</span><span class="lnt"> 2
</span><span class="lnt"> 3
</span><span class="lnt"> 4
</span><span class="lnt"> 5
</span><span class="lnt"> 6
</span><span class="lnt"> 7
</span><span class="lnt"> 8
</span><span class="lnt"> 9
</span><span class="lnt">10
</span><span class="lnt">11
</span><span class="lnt">12
</span><span class="lnt">13
</span><span class="lnt">14
</span><span class="lnt">15
</span><span class="lnt">16
</span><span class="lnt">17
</span><span class="lnt">18
</span><span class="lnt">19
</span><span class="lnt">20
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">vim /etc/sysconfig/network-scripts/ifcfg-ens33

<span class="nv">TYPE</span><span class="o">=</span><span class="s2">&#34;Ethernet&#34;</span>
<span class="nv">PROXY_METHOD</span><span class="o">=</span><span class="s2">&#34;none&#34;</span>
<span class="nv">BROWSER_ONLY</span><span class="o">=</span><span class="s2">&#34;no&#34;</span>
<span class="nv">BOOTPROTO</span><span class="o">=</span><span class="s2">&#34;static&#34;</span>
<span class="nv">DEFROUTE</span><span class="o">=</span><span class="s2">&#34;yes&#34;</span>
<span class="nv">IPV4_FAILURE_FATAL</span><span class="o">=</span><span class="s2">&#34;no&#34;</span>
<span class="nv">IPV6INIT</span><span class="o">=</span><span class="s2">&#34;yes&#34;</span>
<span class="nv">IPV6_AUTOCONF</span><span class="o">=</span><span class="s2">&#34;yes&#34;</span>
<span class="nv">IPV6_DEFROUTE</span><span class="o">=</span><span class="s2">&#34;yes&#34;</span>
<span class="nv">IPV6_FAILURE_FATAL</span><span class="o">=</span><span class="s2">&#34;no&#34;</span>
<span class="nv">IPV6_ADDR_GEN_MODE</span><span class="o">=</span><span class="s2">&#34;stable-privacy&#34;</span>
<span class="nv">NAME</span><span class="o">=</span><span class="s2">&#34;ens33&#34;</span>
<span class="nv">UUID</span><span class="o">=</span><span class="s2">&#34;529588ee-2c22-4dd4-99c1-c823fbf96642&#34;</span>
<span class="nv">DEVICE</span><span class="o">=</span><span class="s2">&#34;ens33&#34;</span>
<span class="nv">ONBOOT</span><span class="o">=</span><span class="s2">&#34;yes&#34;</span>
<span class="nv">IPADDR</span><span class="o">=</span>192.168.198.129
<span class="nv">GATEWAY</span><span class="o">=</span>192.168.198.2
<span class="nv">NETMASK</span><span class="o">=</span>255.255.255.0
</code></pre></td></tr></table>
</div>
</div><p>设置Banana IP地址为 192.168.198.130</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt"> 1
</span><span class="lnt"> 2
</span><span class="lnt"> 3
</span><span class="lnt"> 4
</span><span class="lnt"> 5
</span><span class="lnt"> 6
</span><span class="lnt"> 7
</span><span class="lnt"> 8
</span><span class="lnt"> 9
</span><span class="lnt">10
</span><span class="lnt">11
</span><span class="lnt">12
</span><span class="lnt">13
</span><span class="lnt">14
</span><span class="lnt">15
</span><span class="lnt">16
</span><span class="lnt">17
</span><span class="lnt">18
</span><span class="lnt">19
</span><span class="lnt">20
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">vim /etc/sysconfig/network-scripts/ifcfg-ens33

<span class="nv">TYPE</span><span class="o">=</span><span class="s2">&#34;Ethernet&#34;</span>
<span class="nv">PROXY_METHOD</span><span class="o">=</span><span class="s2">&#34;none&#34;</span>
<span class="nv">BROWSER_ONLY</span><span class="o">=</span><span class="s2">&#34;no&#34;</span>
<span class="nv">BOOTPROTO</span><span class="o">=</span><span class="s2">&#34;static&#34;</span>
<span class="nv">DEFROUTE</span><span class="o">=</span><span class="s2">&#34;yes&#34;</span>
<span class="nv">IPV4_FAILURE_FATAL</span><span class="o">=</span><span class="s2">&#34;no&#34;</span>
<span class="nv">IPV6INIT</span><span class="o">=</span><span class="s2">&#34;yes&#34;</span>
<span class="nv">IPV6_AUTOCONF</span><span class="o">=</span><span class="s2">&#34;yes&#34;</span>
<span class="nv">IPV6_DEFROUTE</span><span class="o">=</span><span class="s2">&#34;yes&#34;</span>
<span class="nv">IPV6_FAILURE_FATAL</span><span class="o">=</span><span class="s2">&#34;no&#34;</span>
<span class="nv">IPV6_ADDR_GEN_MODE</span><span class="o">=</span><span class="s2">&#34;stable-privacy&#34;</span>
<span class="nv">NAME</span><span class="o">=</span><span class="s2">&#34;ens33&#34;</span>
<span class="nv">UUID</span><span class="o">=</span><span class="s2">&#34;529588ee-2c22-4dd4-99c1-c823fbf96642&#34;</span>
<span class="nv">DEVICE</span><span class="o">=</span><span class="s2">&#34;ens33&#34;</span>
<span class="nv">ONBOOT</span><span class="o">=</span><span class="s2">&#34;yes&#34;</span>
<span class="nv">IPADDR</span><span class="o">=</span>192.168.198.130
<span class="nv">GATEWAY</span><span class="o">=</span>192.168.198.2
<span class="nv">NETMASK</span><span class="o">=</span>255.255.255.0
</code></pre></td></tr></table>
</div>
</div><p>设置Cherry IP地址为 192.168.198.131</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt"> 1
</span><span class="lnt"> 2
</span><span class="lnt"> 3
</span><span class="lnt"> 4
</span><span class="lnt"> 5
</span><span class="lnt"> 6
</span><span class="lnt"> 7
</span><span class="lnt"> 8
</span><span class="lnt"> 9
</span><span class="lnt">10
</span><span class="lnt">11
</span><span class="lnt">12
</span><span class="lnt">13
</span><span class="lnt">14
</span><span class="lnt">15
</span><span class="lnt">16
</span><span class="lnt">17
</span><span class="lnt">18
</span><span class="lnt">19
</span><span class="lnt">20
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">vim /etc/sysconfig/network-scripts/ifcfg-ens33

<span class="nv">TYPE</span><span class="o">=</span><span class="s2">&#34;Ethernet&#34;</span>
<span class="nv">PROXY_METHOD</span><span class="o">=</span><span class="s2">&#34;none&#34;</span>
<span class="nv">BROWSER_ONLY</span><span class="o">=</span><span class="s2">&#34;no&#34;</span>
<span class="nv">BOOTPROTO</span><span class="o">=</span><span class="s2">&#34;static&#34;</span>
<span class="nv">DEFROUTE</span><span class="o">=</span><span class="s2">&#34;yes&#34;</span>
<span class="nv">IPV4_FAILURE_FATAL</span><span class="o">=</span><span class="s2">&#34;no&#34;</span>
<span class="nv">IPV6INIT</span><span class="o">=</span><span class="s2">&#34;yes&#34;</span>
<span class="nv">IPV6_AUTOCONF</span><span class="o">=</span><span class="s2">&#34;yes&#34;</span>
<span class="nv">IPV6_DEFROUTE</span><span class="o">=</span><span class="s2">&#34;yes&#34;</span>
<span class="nv">IPV6_FAILURE_FATAL</span><span class="o">=</span><span class="s2">&#34;no&#34;</span>
<span class="nv">IPV6_ADDR_GEN_MODE</span><span class="o">=</span><span class="s2">&#34;stable-privacy&#34;</span>
<span class="nv">NAME</span><span class="o">=</span><span class="s2">&#34;ens33&#34;</span>
<span class="nv">UUID</span><span class="o">=</span><span class="s2">&#34;529588ee-2c22-4dd4-99c1-c823fbf96642&#34;</span>
<span class="nv">DEVICE</span><span class="o">=</span><span class="s2">&#34;ens33&#34;</span>
<span class="nv">ONBOOT</span><span class="o">=</span><span class="s2">&#34;yes&#34;</span>
<span class="nv">IPADDR</span><span class="o">=</span>192.168.198.131
<span class="nv">GATEWAY</span><span class="o">=</span>192.168.198.2
<span class="nv">NETMASK</span><span class="o">=</span>255.255.255.0
</code></pre></td></tr></table>
</div>
</div><h4 id="133-配置dns网络信息">1.3.3 配置DNS网络信息</h4>
<blockquote>
<p>在每个结点执行</p>
</blockquote>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span><span class="lnt">5
</span><span class="lnt">6
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="c1"># 缺少此配置可能无法访问外网</span>
cat &gt;&gt; /etc/resolv.conf <span class="s">&lt;&lt; EOF
</span><span class="s"># Generated by NetworkManager
</span><span class="s">nameserver 8.8.8.8
</span><span class="s">nameserver 114.114.114.114
</span><span class="s">EOF</span>
</code></pre></td></tr></table>
</div>
</div><p>重启生效</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span><span class="lnt">5
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="c1"># 重启网络服务</span>
systemctl restart network

<span class="c1"># 验证网络</span>
ping www.baidu.com
</code></pre></td></tr></table>
</div>
</div><h4 id="134-配置主机名与ip地址解析">1.3.4 配置主机名与IP地址解析</h4>
<blockquote>
<p>在每个结点执行</p>
</blockquote>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span><span class="lnt">5
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">cat &gt;&gt; /etc/hosts <span class="s">&lt;&lt; EOF
</span><span class="s">192.168.198.129 apple
</span><span class="s">192.168.198.130 banana
</span><span class="s">192.168.198.131 cherry
</span><span class="s">EOF</span>
</code></pre></td></tr></table>
</div>
</div><h4 id="135-防火墙配置">1.3.5 防火墙配置</h4>
<blockquote>
<p>在每个结点执行</p>
</blockquote>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span><span class="lnt">5
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="c1"># 关闭防火墙</span>
systemctl stop firewalld

<span class="c1"># 禁止防火墙开机自启</span>
systemctl disable firewalld
</code></pre></td></tr></table>
</div>
</div><h4 id="136-selinux配置">1.3.6 SELinux配置</h4>
<blockquote>
<p>在每个结点执行，修改后需要重启</p>
</blockquote>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span><span class="lnt">5
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="c1"># 永久关闭</span>
sed -i <span class="s1">&#39;s/enforcing/disabled/&#39;</span> /etc/selinux/config

<span class="c1">#临时关闭</span>
setenforce <span class="m">0</span>
</code></pre></td></tr></table>
</div>
</div><p>重启生效</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">reboot
</code></pre></td></tr></table>
</div>
</div><h4 id="137-swap分区配置">1.3.7 swap分区配置</h4>
<blockquote>
<p>在每个结点执行，修改后需重启</p>
</blockquote>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span><span class="lnt">5
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="c1"># 永久关闭</span>
sed -ri <span class="s1">&#39;s/.*swap.*/#&amp;/&#39;</span> /etc/fstab

<span class="c1"># 临时关闭</span>
swapoff -a
</code></pre></td></tr></table>
</div>
</div><p>重启生效</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">reboot
</code></pre></td></tr></table>
</div>
</div><h4 id="138-配置内核转发及网桥过滤">1.3.8 配置内核转发及网桥过滤</h4>
<blockquote>
<p>在每个结点执行</p>
</blockquote>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span><span class="lnt">5
</span><span class="lnt">6
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">cat &gt; /etc/sysctl.d/k8s.conf <span class="s">&lt;&lt; EOF
</span><span class="s">net.bridge.bridge-nf-call-ip6tables = 1
</span><span class="s">net.bridge.bridge-nf-call-iptables = 1
</span><span class="s">net.ipv4.ip_forward = 1
</span><span class="s">vm.swappiness = 0
</span><span class="s">EOF</span>
</code></pre></td></tr></table>
</div>
</div><div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span><span class="lnt">5
</span><span class="lnt">6
</span><span class="lnt">7
</span><span class="lnt">8
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="c1"># 加载br_netfilter模块</span>
modprobe br_netfilter

<span class="c1"># 查看是否加载</span>
lsmod <span class="p">|</span> grep br_netfilter

<span class="c1"># 生效</span>
sysctl --system  
</code></pre></td></tr></table>
</div>
</div><h4 id="139-时间同步配置">1.3.9 时间同步配置</h4>
<blockquote>
<p>在每个结点执行</p>
</blockquote>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span><span class="lnt">5
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="c1"># 安装 ntpdate</span>
yum install ntpdate -y

<span class="c1"># 同步时间</span>
ntpdate time.windows.com
</code></pre></td></tr></table>
</div>
</div><h4 id="1310-ipvs配置">1.3.10 IPVS配置</h4>
<blockquote>
<p>在每个结点执行</p>
</blockquote>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt"> 1
</span><span class="lnt"> 2
</span><span class="lnt"> 3
</span><span class="lnt"> 4
</span><span class="lnt"> 5
</span><span class="lnt"> 6
</span><span class="lnt"> 7
</span><span class="lnt"> 8
</span><span class="lnt"> 9
</span><span class="lnt">10
</span><span class="lnt">11
</span><span class="lnt">12
</span><span class="lnt">13
</span><span class="lnt">14
</span><span class="lnt">15
</span><span class="lnt">16
</span><span class="lnt">17
</span><span class="lnt">18
</span><span class="lnt">19
</span><span class="lnt">20
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="c1"># 安装ipset和ipvsadm</span>
yum -y install ipset ipvsadm

cat &gt; /etc/sysconfig/modules/ipvs.modules <span class="s">&lt;&lt;EOF
</span><span class="s">#!/bin/bash
</span><span class="s">modprobe -- ip_vs
</span><span class="s">modprobe -- ip_vs_rr
</span><span class="s">modprobe -- ip_vs_wrr
</span><span class="s">modprobe -- ip_vs_sh
</span><span class="s">modprobe -- nf_conntrack_ipv4
</span><span class="s">EOF</span>

<span class="c1"># 添加执行权限</span>
chmod <span class="m">755</span> /etc/sysconfig/modules/ipvs.modules

<span class="c1"># 运行</span>
bash /etc/sysconfig/modules/ipvs.modules

<span class="c1"># 检查是否加载</span>
lsmod <span class="p">|</span> grep -e ipvs -e nf_conntrack_ipv4
</code></pre></td></tr></table>
</div>
</div><h4 id="1311-升级操作系统内核">1.3.11 升级操作系统内核</h4>
<blockquote>
<p>在每个结点执行，升级后需重启</p>
</blockquote>
<p>安装YUM源</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span><span class="lnt">5
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="c1"># 导入elrepo gpg key</span>
rpm --import https://www.elrepo.org/RPM-GPG-KEY-elrepo.org

<span class="c1"># 安装elrepo YUM源仓库</span>
yum -y install https://www.elrepo.org/elrepo-release-7.0-4.el7.elrepo.noarch.rpm
</code></pre></td></tr></table>
</div>
</div><p>升级内核版本</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt"> 1
</span><span class="lnt"> 2
</span><span class="lnt"> 3
</span><span class="lnt"> 4
</span><span class="lnt"> 5
</span><span class="lnt"> 6
</span><span class="lnt"> 7
</span><span class="lnt"> 8
</span><span class="lnt"> 9
</span><span class="lnt">10
</span><span class="lnt">11
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="c1"># 稳定版kernel-ml 长期维护版本kernel-lt  </span>
yum -y --enablerepo<span class="o">=</span>elrepo-kernel  install  kernel-ml

<span class="c1"># 查看已安装的内核</span>
rpm -qa <span class="p">|</span> grep kernel

<span class="c1"># 查看默认内核</span>
grubby --default-kernel

<span class="c1"># 若不是最新版本时执行</span>
grubby --set-default <span class="k">$(</span>ls /boot/vmlinuz-* <span class="p">|</span> grep elrepo<span class="k">)</span>
</code></pre></td></tr></table>
</div>
</div><p>重启生效</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">reboot
</code></pre></td></tr></table>
</div>
</div><p>至此，操作系统环境准备已完成，下一步可以进行Docker、容器运行时以及Kubernetes软件安装和配置！</p>
<h3 id="14-创建快照">1.4 创建快照</h3>
<div class="details admonition tip open">
        <div class="details-summary admonition-title">
            <i class="icon fas fa-lightbulb fa-fw"></i>技巧<i class="details-icon fas fa-angle-right fa-fw"></i>
        </div>
        <div class="details-content">
            <div class="admonition-content"><i class="far fa-bookmark fa-fw"></i>&nbsp;在VMware对3台虚拟机创建一份快照，便于后续配置出现问题时回滚！</div>
        </div>
    </div>
<div class="mermaid" id="id-1"></div>
<h2 id="2-容器运行时准备">2 容器运行时准备</h2>
<blockquote>
<p>在每个结点执行</p>
</blockquote>
<h3 id="21-安装docker">2.1 安装Docker</h3>
<blockquote>
<p>参考：清华大学开源软件镜像站 <a href="https://mirror.tuna.tsinghua.edu.cn/help/docker-ce/" target="_blank" rel="noopener noreffer">Docker CE 软件仓库</a></p>
</blockquote>
<p>因作者本地为<code>CentOS7.9</code>操作系统环境，这里使用<code>CentOS/RHEL</code>版本。</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt"> 1
</span><span class="lnt"> 2
</span><span class="lnt"> 3
</span><span class="lnt"> 4
</span><span class="lnt"> 5
</span><span class="lnt"> 6
</span><span class="lnt"> 7
</span><span class="lnt"> 8
</span><span class="lnt"> 9
</span><span class="lnt">10
</span><span class="lnt">11
</span><span class="lnt">12
</span><span class="lnt">13
</span><span class="lnt">14
</span><span class="lnt">15
</span><span class="lnt">16
</span><span class="lnt">17
</span><span class="lnt">18
</span><span class="lnt">19
</span><span class="lnt">20
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="c1"># 如果你之前安装过docker，请先删掉</span>
yum remove docker <span class="se">\
</span><span class="se"></span>          docker-client <span class="se">\
</span><span class="se"></span>          docker-client-latest <span class="se">\
</span><span class="se"></span>          docker-common <span class="se">\
</span><span class="se"></span>          docker-latest <span class="se">\
</span><span class="se"></span>          docker-latest-logrotate <span class="se">\
</span><span class="se"></span>          docker-logrotate <span class="se">\
</span><span class="se"></span>          docker-engine

<span class="c1"># 安装依赖，下载repo文件，并把软件仓库地址替换为镜像站：</span>
yum install -y yum-utils

<span class="c1"># 这里从docker官方下载文件可能会失败，修改为从阿里云下载</span>
<span class="c1"># yum-config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo</span>
yum-config-manager --add-repo http://mirrors.aliyun.com/docker-ce/linux/centos/docker-ce.repo
sed -i <span class="s1">&#39;s+https://download.docker.com+https://mirrors.tuna.tsinghua.edu.cn/docker-ce+&#39;</span> /etc/yum.repos.d/docker-ce.repo

<span class="c1"># 安装Docker及相关插件</span>
yum install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin -y
</code></pre></td></tr></table>
</div>
</div><h3 id="22-配置镜像加速">2.2 配置镜像加速</h3>
<blockquote>
<p>参考：中科大USTC镜像站 <a href="https://mirrors.ustc.edu.cn/help/dockerhub.html" target="_blank" rel="noopener noreffer">Docker Hub 源使用帮助</a></p>
</blockquote>
<div class="details admonition note open">
        <div class="details-summary admonition-title">
            <i class="icon fas fa-pencil-alt fa-fw"></i>注意<i class="details-icon fas fa-angle-right fa-fw"></i>
        </div>
        <div class="details-content">
            <div class="admonition-content"><p>截止2024年7月，以下镜像加速已不可用</p>
<ul>
<li>腾讯云 <a href="https://mirror.ccs.tencentyun.com" target="_blank" rel="noopener noreffer">https://mirror.ccs.tencentyun.com</a></li>
<li>中科大 <a href="https://docker.mirrors.ustc.edu.cn" target="_blank" rel="noopener noreffer">https://docker.mirrors.ustc.edu.cn</a></li>
<li>Docker官方 <a href="https://registry.docker-cn.com" target="_blank" rel="noopener noreffer">https://registry.docker-cn.com</a></li>
<li>网易云 <a href="http://hub-mirror.c.163.com" target="_blank" rel="noopener noreffer">http://hub-mirror.c.163.com</a></li>
<li>南大 <a href="https://docker.nju.edu.cn" target="_blank" rel="noopener noreffer">https://docker.nju.edu.cn</a></li>
</ul>
<p>以下镜像加速仍可用</p>
<ul>
<li>Daocloud <a href="https://docker.m.daocloud.io" target="_blank" rel="noopener noreffer">https://docker.m.daocloud.io</a></li>
<li>华为云 <a href="https://swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io" target="_blank" rel="noopener noreffer">https://swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io</a></li>
</ul>
</div>
        </div>
    </div>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt"> 1
</span><span class="lnt"> 2
</span><span class="lnt"> 3
</span><span class="lnt"> 4
</span><span class="lnt"> 5
</span><span class="lnt"> 6
</span><span class="lnt"> 7
</span><span class="lnt"> 8
</span><span class="lnt"> 9
</span><span class="lnt">10
</span><span class="lnt">11
</span><span class="lnt">12
</span><span class="lnt">13
</span><span class="lnt">14
</span><span class="lnt">15
</span><span class="lnt">16
</span><span class="lnt">17
</span><span class="lnt">18
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="c1"># 在/etc路径下新建docker文件夹</span>
sudo mkdir -p /etc/docker

<span class="c1"># 在docker目录下新建daemon.json文件，并加入registry-mirrors</span>
sudo tee /etc/docker/daemon.json <span class="s">&lt;&lt;-&#39;EOF&#39;
</span><span class="s">{
</span><span class="s">  &#34;registry-mirrors&#34;: [&#34;https://docker.m.daocloud.io&#34;]
</span><span class="s">}
</span><span class="s">EOF</span>

<span class="c1"># 重新加载systemd管理的单位文件</span>
sudo systemctl daemon-reload

<span class="c1"># 重新启动Docker服务</span>
sudo systemctl restart docker

<span class="c1"># 设置Docker服务开机自启</span>
systemctl <span class="nb">enable</span> docker
</code></pre></td></tr></table>
</div>
</div><h3 id="23-安装cri-docker">2.3 安装cri-docker</h3>
<blockquote>
<p>采用软件包离线安装方式</p>
</blockquote>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt"> 1
</span><span class="lnt"> 2
</span><span class="lnt"> 3
</span><span class="lnt"> 4
</span><span class="lnt"> 5
</span><span class="lnt"> 6
</span><span class="lnt"> 7
</span><span class="lnt"> 8
</span><span class="lnt"> 9
</span><span class="lnt">10
</span><span class="lnt">11
</span><span class="lnt">12
</span><span class="lnt">13
</span><span class="lnt">14
</span><span class="lnt">15
</span><span class="lnt">16
</span><span class="lnt">17
</span><span class="lnt">18
</span><span class="lnt">19
</span><span class="lnt">20
</span><span class="lnt">21
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="c1"># 下载cri-docker.rpm包</span>
<span class="c1"># 可能无法直接从Github下载软件包，需自行下载拷贝到虚拟机</span>
https://github.com/Mirantis/cri-dockerd/releases/download/v0.3.8/cri-dockerd-0.3.8-3.el7.x86_64.rpm

<span class="c1"># 安装libcgroup</span>
yum install -y libcgroup

<span class="c1"># 安装cri-docker</span>
rpm -ivh cri-dockerd-0.3.8-3.el7.x86_64.rpm

<span class="c1"># 修改cri-docker.service配置第10行</span>
vi /usr/lib/systemd/system/cri-docker.service

修改为以下配置
<span class="nv">ExecStart</span><span class="o">=</span>/usr/bin/cri-dockerd --pod-infra-container-image<span class="o">=</span>registry.aliyuncs.com/google_containers/pause:3.9 --container-runtime-endpoint fd://

<span class="c1"># 启动cri-docker服务</span>
systemctl start cri-docker

<span class="c1"># 设置cri-docker服务开机自启</span>
systemctl <span class="nb">enable</span> cri-docker
</code></pre></td></tr></table>
</div>
</div><h2 id="3-部署kubernetes集群">3 部署Kubernetes集群</h2>
<h3 id="31-配置阿里云镜像源代理">3.1 配置阿里云镜像源代理</h3>
<blockquote>
<p>在每个结点执行</p>
</blockquote>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt"> 1
</span><span class="lnt"> 2
</span><span class="lnt"> 3
</span><span class="lnt"> 4
</span><span class="lnt"> 5
</span><span class="lnt"> 6
</span><span class="lnt"> 7
</span><span class="lnt"> 8
</span><span class="lnt"> 9
</span><span class="lnt">10
</span><span class="lnt">11
</span><span class="lnt">12
</span><span class="lnt">13
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">cat <span class="s">&lt;&lt;EOF &gt; /etc/yum.repos.d/kubernetes.repo
</span><span class="s">[kubernetes]
</span><span class="s">name=Kubernetes
</span><span class="s">baseurl=https://mirrors.aliyun.com/kubernetes/yum/repos/kubernetes-el7-x86_64/
</span><span class="s">enabled=1
</span><span class="s">gpgcheck=1
</span><span class="s">repo_gpgcheck=1
</span><span class="s">gpgkey=https://mirrors.aliyun.com/kubernetes/yum/doc/yum-key.gpg https://mirrors.aliyun.com/kubernetes/yum/doc/rpm-package-key.gpg
</span><span class="s">EOF</span>

yum makecache
yum install -y --nogpgcheck kubelet kubeadm kubectl
yum list kubelet --showduplicates <span class="p">|</span> sort -r <span class="p">|</span>grep 1.28
</code></pre></td></tr></table>
</div>
</div><h3 id="32-安装kubeadmkubeletkubectl">3.2 安装kubeadm、kubelet、kubectl</h3>
<blockquote>
<p>在每个结点执行</p>
</blockquote>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt"> 1
</span><span class="lnt"> 2
</span><span class="lnt"> 3
</span><span class="lnt"> 4
</span><span class="lnt"> 5
</span><span class="lnt"> 6
</span><span class="lnt"> 7
</span><span class="lnt"> 8
</span><span class="lnt"> 9
</span><span class="lnt">10
</span><span class="lnt">11
</span><span class="lnt">12
</span><span class="lnt">13
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="c1"># 安装最新版本</span>
yum install -y kubelet kubeadm kubectl -y

<span class="c1"># 配置kubectl命令补全</span>
yum install bash-completion -y
kubectl completion bash &gt; /etc/profile.d/kubectl_completion.sh
. /etc/profile.d/kubectl_completion.sh

<span class="c1"># 启动kubelet服务</span>
systemctl start kubelet

<span class="c1"># 设置kubelet开机自启</span>
systemctl <span class="nb">enable</span> kubelet 
</code></pre></td></tr></table>
</div>
</div><h3 id="33-准备启动镜像">3.3 准备启动镜像</h3>
<blockquote>
<p>配置从阿里云镜像源拉取</p>
</blockquote>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span><span class="lnt">5
</span><span class="lnt">6
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash">kubeadm config images list --kubernetes-version<span class="o">=</span>v1.28.2
 
kubeadm config images list --image-repository registry.aliyuncs.com/google_containers --kubernetes-version<span class="o">=</span>v1.28.2

<span class="c1"># 可能会卡在这里，加--v=5查看日志，可忽略该步，直接执行下面的kubeadm init xxx</span>
kubeadm config images pull --image-repository registry.aliyuncs.com/google_containers --kubernetes-version<span class="o">=</span>v1.28.2 --cri-socket<span class="o">=</span>unix:///var/run/cri-dockerd.sock
</code></pre></td></tr></table>
</div>
</div><h3 id="34-初始化master结点">3.4 初始化Master结点</h3>
<blockquote>
<p>仅在Master结点执行</p>
</blockquote>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt"> 1
</span><span class="lnt"> 2
</span><span class="lnt"> 3
</span><span class="lnt"> 4
</span><span class="lnt"> 5
</span><span class="lnt"> 6
</span><span class="lnt"> 7
</span><span class="lnt"> 8
</span><span class="lnt"> 9
</span><span class="lnt">10
</span><span class="lnt">11
</span><span class="lnt">12
</span><span class="lnt">13
</span><span class="lnt">14
</span><span class="lnt">15
</span><span class="lnt">16
</span><span class="lnt">17
</span><span class="lnt">18
</span><span class="lnt">19
</span><span class="lnt">20
</span><span class="lnt">21
</span><span class="lnt">22
</span><span class="lnt">23
</span><span class="lnt">24
</span><span class="lnt">25
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="o">[</span>root@apple ~<span class="o">]</span><span class="c1"># kubeadm init --kubernetes-version=v1.28.2  --image-repository registry.aliyuncs.com/google_containers --cri-socket=unix:///var/run/cri-dockerd.sock</span>

<span class="c1"># 按照提示完成以下配置</span>
....
Your Kubernetes control-plane has initialized successfully!

To start using your cluster, you need to run the following as a regular user:

  mkdir -p <span class="nv">$HOME</span>/.kube
  sudo cp -i /etc/kubernetes/admin.conf <span class="nv">$HOME</span>/.kube/config
  sudo chown <span class="k">$(</span>id -u<span class="k">)</span>:<span class="k">$(</span>id -g<span class="k">)</span> <span class="nv">$HOME</span>/.kube/config

Alternatively, <span class="k">if</span> you are the root user, you can run:

  <span class="nb">export</span> <span class="nv">KUBECONFIG</span><span class="o">=</span>/etc/kubernetes/admin.conf

You should now deploy a pod network to the cluster.
Run <span class="s2">&#34;kubectl apply -f [podnetwork].yaml&#34;</span> with one of the options listed at:
  https://kubernetes.io/docs/concepts/cluster-administration/addons/

Then you can join any number of worker nodes by running the following on each as root:

kubeadm join 192.168.198.129:6443 --token 3o9lba.lm3tk8w0muttgth7 <span class="se">\
</span><span class="se"></span>        --discovery-token-ca-cert-hash sha256:4d8ba51c787dd9fdaf0060573a14ad25c85eba1dc08c1b5f2d91d823185fdb22
....
</code></pre></td></tr></table>
</div>
</div><h3 id="35-将slave结点加入集群">3.5 将Slave结点加入集群</h3>
<blockquote>
<p>在Slave结点执行</p>
</blockquote>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span><span class="lnt">5
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="c1"># 加入worker结点</span>
kubeadm join 192.168.198.129:6443 --token 3o9lba.lm3tk8w0muttgth7 --discovery-token-ca-cert-hash sha256:4d8ba51c787dd9fdaf0060573a14ad25c85eba1dc08c1b5f2d91d823185fdb22 --cri-socket<span class="o">=</span>unix:///var/run/cri-dockerd.sock

<span class="c1"># 若忘记，可在master结点执行以下命令，重新获取</span>
kubeadm token create --print-join-command
</code></pre></td></tr></table>
</div>
</div><blockquote>
<p>在Master结点执行查看结点状态命令，Master和Slave都处于<code>NotReady</code>状态</p>
</blockquote>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span><span class="lnt">5
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-sh" data-lang="sh"><span class="o">[</span>root@apple ~<span class="o">]</span><span class="c1"># kubectl get nodes</span>
NAME     STATUS     ROLES           AGE     VERSION
apple    NotReady   control-plane   3m47s   v1.28.2
banana   NotReady   &lt;none&gt;          2m29s   v1.28.2
cherry   NotReady   &lt;none&gt;          17s     v1.28.2
</code></pre></td></tr></table>
</div>
</div><h3 id="36-安装cni网络接口插件">3.6 安装CNI网络接口插件</h3>
<div class="details admonition note open">
        <div class="details-summary admonition-title">
            <i class="icon fas fa-pencil-alt fa-fw"></i>CNI网络插件<i class="details-icon fas fa-angle-right fa-fw"></i>
        </div>
        <div class="details-content">
            <div class="admonition-content"><p>Flannel和Calico都是Kubernetes流行的容器网络接口（CNI）插件（除此之外还有Canal、Weave等），用于在集群中的 pod 之间提供网络连接，它们都具有自己的优缺点，适合不同的用例。</p>
<p><strong>Flannel</strong>是一种简单、易用的 overlay 网络解决方案。它使用虚拟网络技术在主机之间创建隧道，使 pod 能够相互通信，即使它们位于不同的子网或物理主机上。</p>
<p><strong>Calico</strong> 是一种功能更全面的 CNI 插件，它提供路由、策略和可观察性等功能。它使用 BGP 协议在主机之间路由流量，这使其具有高度的可扩展性和可管理性。</p>
<p>深入了解可参考<a href="https://kubernetes.feisky.xyz/extension/network" target="_blank" rel="noopener noreffer">Kubernetes指南-网络插件</a></p>
</div>
        </div>
    </div>
<blockquote>
<p>这里安装使用Calico网络接口插件，以下命令仅在Master结点执行</p>
</blockquote>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span><span class="lnt">5
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-sh" data-lang="sh"><span class="c1"># 安装wget命令</span>
yum install -y wget

<span class="c1"># 下载Calico插件部署yaml</span>
wget https://docs.projectcalico.org/manifests/calico.yaml
</code></pre></td></tr></table>
</div>
</div><p>修改calico.yaml中CALICO_IPV4POOL_CIDR参数，该配置参数值应与Pod网段<code>pod-network-cidr</code>一致，若kubectl init中未指定<code>pod-network-cidr</code>,默认Pod网段为<code>10.244.0.0/16</code>，此配置默认为注释需要取消</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span><span class="lnt">5
</span><span class="lnt">6
</span><span class="lnt">7
</span><span class="lnt">8
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-sh" data-lang="sh">vi calico.yaml

<span class="c1"># 修改后如下</span>
···
<span class="c1"># no effect. This should fall within `--cluster-cidr`.</span>
- name: CALICO_IPV4POOL_CIDR
  value: <span class="s2">&#34;10.244.0.0/16&#34;</span>
···
</code></pre></td></tr></table>
</div>
</div><p>因docker.io国内访问受限，需修改默认拉取的镜像地址，通过其他镜像站加速，这里使用华为云</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-sh" data-lang="sh"><span class="c1"># 执行全局sed替换</span>
sed -i <span class="s1">&#39;s/docker.io/swr.cn-north-4.myhuaweicloud.com\/ddn-k8s\/docker.io/g&#39;</span> calico.yaml
</code></pre></td></tr></table>
</div>
</div><p>部署安装Calico插件，因拉取镜像慢，需等待一段时间再观察Pod状态</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt"> 1
</span><span class="lnt"> 2
</span><span class="lnt"> 3
</span><span class="lnt"> 4
</span><span class="lnt"> 5
</span><span class="lnt"> 6
</span><span class="lnt"> 7
</span><span class="lnt"> 8
</span><span class="lnt"> 9
</span><span class="lnt">10
</span><span class="lnt">11
</span><span class="lnt">12
</span><span class="lnt">13
</span><span class="lnt">14
</span><span class="lnt">15
</span><span class="lnt">16
</span><span class="lnt">17
</span><span class="lnt">18
</span><span class="lnt">19
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-sh" data-lang="sh"><span class="c1"># 部署calico插件</span>
kubectl apply -f calico.yaml

<span class="c1"># 查看kube-system命名空间Pod状态</span>
<span class="o">[</span>root@apple ~<span class="o">]</span><span class="c1"># kubectl get pods -n kube-system</span>
NAME                                       READY   STATUS    RESTARTS      AGE
calico-kube-controllers-665548954f-9qlzn   1/1     Running   <span class="m">0</span>             6m43s
calico-node-c66l9                          1/1     Running   <span class="m">0</span>             6m43s
calico-node-mjblm                          1/1     Running   <span class="m">0</span>             6m43s
calico-node-xtwbc                          1/1     Running   <span class="m">0</span>             6m43s
coredns-66f779496c-9996t                   1/1     Running   <span class="m">0</span>             31h
coredns-66f779496c-ft72m                   1/1     Running   <span class="m">0</span>             31h
etcd-apple                                 1/1     Running   <span class="m">2</span> <span class="o">(</span>88m ago<span class="o">)</span>   31h
kube-apiserver-apple                       1/1     Running   <span class="m">2</span> <span class="o">(</span>88m ago<span class="o">)</span>   31h
kube-controller-manager-apple              1/1     Running   <span class="m">2</span> <span class="o">(</span>88m ago<span class="o">)</span>   31h
kube-proxy-brb6b                           1/1     Running   <span class="m">2</span> <span class="o">(</span>87m ago<span class="o">)</span>   31h
kube-proxy-f9zlq                           1/1     Running   <span class="m">2</span> <span class="o">(</span>87m ago<span class="o">)</span>   31h
kube-proxy-s2blk                           1/1     Running   <span class="m">2</span> <span class="o">(</span>88m ago<span class="o">)</span>   31h
kube-scheduler-apple                       1/1     Running   <span class="m">2</span> <span class="o">(</span>88m ago<span class="o">)</span>   31h
</code></pre></td></tr></table>
</div>
</div><p>可以看到，Kubernetes集群的核心组件都已正常工作，在Master结点可查看集群结点都处于<code>Ready</code>状态</p>
<div class="highlight"><div class="chroma">
<table class="lntable"><tr><td class="lntd">
<pre tabindex="0" class="chroma"><code><span class="lnt">1
</span><span class="lnt">2
</span><span class="lnt">3
</span><span class="lnt">4
</span><span class="lnt">5
</span></code></pre></td>
<td class="lntd">
<pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="o">[</span>root@apple ~<span class="o">]</span><span class="c1"># kubectl get nodes </span>
NAME     STATUS   ROLES           AGE   VERSION
apple    Ready    control-plane   32h   v1.28.2
banana   Ready    &lt;none&gt;          32h   v1.28.2
cherry   Ready    &lt;none&gt;          32h   v1.28.2
</code></pre></td></tr></table>
</div>
</div><blockquote>
<p>到这里，一个单Master双Slave的Kubernetes集群就搭建完成啦！</p>
</blockquote>]]></description>
</item><item>
    <title>Golang 基础知识笔记</title>
    <link>http://leoyim.cn/golang-basic-notes/</link>
    <pubDate>Sat, 09 Sep 2023 09:28:29 &#43;0800</pubDate>
    <author>作者</author>
    <guid>http://leoyim.cn/golang-basic-notes/</guid>
    <description><![CDATA[<p>题图源：<a href="https://technology.riotgames.com/news/leveraging-golang-game-development-and-operations" title="LEVERAGING GOLANG FOR GAME DEVELOPMENT AND OPERATIONS" target="_blank" rel="noopener noreffer">LEVERAGING GOLANG FOR GAME DEVELOPMENT AND OPERATIONS</a></p>
]]></description>
</item></channel>
</rss>
